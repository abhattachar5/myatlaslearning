# Netlify config — static site, no build step.
[build]
  publish = "."
  functions = "netlify/functions"

# SPA-style pages are plain .html, so no redirects needed.
# Functions run on the Node runtime; ANTHROPIC_API_KEY is set in the Netlify UI.
