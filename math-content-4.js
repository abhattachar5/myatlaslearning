// math-content-4.js â€" Flashcards & Questions for Topics 14-17
// Topic 14: Order of Operations | Topic 15: Rational Numbers
// Topic 16: Problem Solving & Estimation | Topic 17: Units of Measurement

// â"€â"€â"€ TOPIC 14: ORDER OF OPERATIONS â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€

FLASHCARDS.push(
  // mi-14-1: Order of Operations: Whole Numbers
  { islandId:'mi-14-1', front:'What does BIDMAS stand for?', back:'Brackets, Indices, Division, Multiplication, Addition, Subtraction â€" the order operations must be performed.' },
  { islandId:'mi-14-1', front:'Evaluate: 3 + 4 Ã— 2', back:'Multiplication first: 4Ã—2=8. Then add: 3+8 = 11. (Not 14!)' },
  { islandId:'mi-14-1', front:'Why does 6 Ã· 2 Ã— 3 = 9, not 1?', back:'Division and multiplication have equal priority â€" work left to right. 6Ã·2=3, then 3Ã—3=9.' },

  // mi-14-2: Order of Operations: Decimals & Fractions
  { islandId:'mi-14-2', front:'Evaluate: (1.5 + 0.5) Ã— 4', back:'Brackets first: 1.5+0.5=2. Then: 2Ã—4 = 8.' },
  { islandId:'mi-14-2', front:'Evaluate: Â½ + Â¼ Ã— 8', back:'Multiplication first: Â¼Ã—8=2. Then: Â½+2 = 2Â½.' },
  { islandId:'mi-14-2', front:'Evaluate: 2.4 Ã· (0.6 Ã— 2)', back:'Brackets first: 0.6Ã—2=1.2. Then: 2.4Ã·1.2 = 2.' },

  // mi-14-3: Order of Operations: Integers
  { islandId:'mi-14-3', front:'Evaluate: âˆ'3 + 4 Ã— (âˆ'2)', back:'Multiplication first: 4Ã—(âˆ'2)=âˆ'8. Then: âˆ'3+(âˆ'8) = âˆ'11.' },
  { islandId:'mi-14-3', front:'Evaluate: (âˆ'5)Â² âˆ' 10', back:'Indices first: (âˆ'5)Â²=25. Then: 25âˆ'10 = 15.' },
  { islandId:'mi-14-3', front:'Evaluate: 12 Ã· (âˆ'3) + 7', back:'Brackets (already evaluated): 12Ã·(âˆ'3)=âˆ'4. Then: âˆ'4+7 = 3.' }
);

// â"€â"€â"€ TOPIC 15: RATIONAL NUMBERS â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€

FLASHCARDS.push(
  // mi-15-1: Rational Numbers on Number Lines
  { islandId:'mi-15-1', front:'What is a rational number?', back:'Any number that can be written as a fraction p/q where p and q are integers and q â‰  0. Includes integers, fractions, and terminating/recurring decimals.' },
  { islandId:'mi-15-1', front:'Plot âˆ'Â¾ on a number line. Where does it sit?', back:'Between âˆ'1 and 0, three-quarters of the way from 0 toward âˆ'1.' },
  { islandId:'mi-15-1', front:'Is 0.333â€¦ a rational number?', back:'Yes â€" it equals â…", which is p/q form.' },

  // mi-15-2: Compare & Order Rational Numbers
  { islandId:'mi-15-2', front:'Which is greater: âˆ'â…– or âˆ'â…"?', back:'Convert: âˆ'â…–=âˆ'0.4, âˆ'â…"â‰ˆâˆ'0.333. Since âˆ'0.333 > âˆ'0.4, âˆ'â…" is greater.' },
  { islandId:'mi-15-2', front:'Order: 0.7, Â¾, 71%', back:'0.7=70%, Â¾=75%, 71%=71%. Order: 0.7 < 71% < Â¾.' },
  { islandId:'mi-15-2', front:'How do you compare two negative fractions?', back:'Convert to decimals or a common denominator, then remember: the less negative number is greater.' },

  // mi-15-3: Rational Number Problems
  { islandId:'mi-15-3', front:'A temperature drops from 2Â½Â°C to âˆ'1Â¾Â°C. What is the change?', back:'Change = âˆ'1Â¾ âˆ' 2Â½ = âˆ'4Â¼Â°C (a drop of 4Â¼ degrees).' },
  { islandId:'mi-15-3', front:'What is the absolute value of âˆ'3.6?', back:'|âˆ'3.6| = 3.6 (distance from zero, always positive).' },
  { islandId:'mi-15-3', front:'Between which two consecutive integers does âˆ'7/3 lie?', back:'âˆ'7/3 â‰ˆ âˆ'2.33. It lies between âˆ'3 and âˆ'2.' }
);

// â"€â"€â"€ TOPIC 16: PROBLEM SOLVING & ESTIMATION â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€

FLASHCARDS.push(
  // mi-16-1: Multi-Step Word Problems
  { islandId:'mi-16-1', front:'What are the steps for solving a word problem?', back:'1. Read carefully. 2. Identify key info. 3. Choose operations. 4. Calculate. 5. Check your answer makes sense.' },
  { islandId:'mi-16-1', front:'A cinema has 24 rows of 35 seats. 612 tickets are sold. How many seats are empty?', back:'Total = 24Ã—35=840. Empty = 840âˆ'612 = 228 seats.' },
  { islandId:'mi-16-1', front:'What does "working backwards" mean in problem solving?', back:'Starting from the given answer and reversing each operation to find an unknown starting value.' },

  // mi-16-2: Estimation Strategies
  { islandId:'mi-16-2', front:'Estimate 487 Ã— 32 using rounding to 1 s.f.', back:'â‰ˆ 500 Ã— 30 = 15,000. (Actual: 15,584 â€" estimate is reasonable.)' },
  { islandId:'mi-16-2', front:'What is "rounding to 1 significant figure"?', back:'Round each number so only the first non-zero digit remains. E.g. 4,763 â†' 5,000.' },
  { islandId:'mi-16-2', front:'When is estimation useful?', back:'To check if a calculator answer is reasonable, or to get a quick approximate answer without exact calculation.' },

  // mi-16-3: Logical Reasoning Problems
  { islandId:'mi-16-3', front:'What is a counter-example?', back:'A single example that disproves a general statement. E.g. "all primes are odd" â€" counter-example: 2 is prime and even.' },
  { islandId:'mi-16-3', front:'Sam thinks of a number, doubles it, adds 6, and gets 18. What is the number?', back:'Work backwards: 18âˆ'6=12, 12Ã·2=6. The number is 6.' },
  { islandId:'mi-16-3', front:'What is a Venn diagram used for in problem solving?', back:'Organising information about overlapping groups or sets to avoid double-counting.' },

  // mi-16-4: Extra/Missing Info & Direction
  { islandId:'mi-16-4', front:'What is "extra information" in a word problem?', back:'Data given in the problem that is NOT needed to find the answer.\n\nExample: "Sam has 5 red and 3 blue balls. He buys 2 more red. How many red does he have now?"\nExtra info: 3 blue balls (not needed).\nAnswer: 5 + 2 = 7 red.' },
  { islandId:'mi-16-4', front:'What is "missing information" in a word problem?', back:'When a problem does not give you enough data to find the answer.\n\nExample: "A shop sells pencils for 30p each. How much does Sarah spend?"\nMissing: how many pencils Sarah buys.\n\nYou must identify WHAT is missing and explain why you cannot solve without it.' },
  { islandId:'mi-16-4', front:'How do you find the distance and direction back to a starting point?', back:'Track each leg of the journey on a number line or grid.\n\nExample: Walk 5 km east, then 3 km west.\nNet displacement = 5 − 3 = 2 km east.\nTo return: walk 2 km WEST.\n\nAlways find the NET displacement, then reverse the direction.' }
);

// â"€â"€â"€ TOPIC 17: UNITS OF MEASUREMENT â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€

FLASHCARDS.push(
  // mi-17-1: Metric Units
  { islandId:'mi-17-1', front:'What are the metric prefixes for kilo-, centi-, milli-?', back:'kilo- = Ã—1000 | centi- = Ã·100 | milli- = Ã·1000. E.g. 1 km = 1000 m, 1 m = 100 cm = 1000 mm.' },
  { islandId:'mi-17-1', front:'Convert 3.5 km to metres.', back:'3.5 Ã— 1000 = 3,500 m.' },
  { islandId:'mi-17-1', front:'Convert 450 ml to litres.', back:'450 Ã· 1000 = 0.45 litres.' },

  // mi-17-2: Imperial Units
  { islandId:'mi-17-2', front:'How many inches in a foot? Feet in a yard?', back:'12 inches = 1 foot. 3 feet = 1 yard.' },
  { islandId:'mi-17-2', front:'How many pounds in a stone? Ounces in a pound?', back:'14 pounds = 1 stone. 16 ounces = 1 pound.' },
  { islandId:'mi-17-2', front:'How many pints in a gallon?', back:'8 pints = 1 gallon.' },

  // mi-17-3: Convert Metric & Imperial
  { islandId:'mi-17-3', front:'Approximate: 1 kg â‰ˆ ? pounds', back:'1 kg â‰ˆ 2.2 pounds.' },
  { islandId:'mi-17-3', front:'Approximate: 1 mile â‰ˆ ? km', back:'1 mile â‰ˆ 1.6 km.' },
  { islandId:'mi-17-3', front:'Convert 5 miles to km (use 1 mile â‰ˆ 1.6 km).', back:'5 Ã— 1.6 = 8 km.' },

  // mi-17-4: Area/Volume Units & Temperature
  { islandId:'mi-17-4', front:'How do you convert cm² to m²?', back:'1 m = 100 cm, so 1 m² = 100 × 100 = 10,000 cm².\n\nTo convert cm² → m²: divide by 10,000.\nTo convert m² → cm²: multiply by 10,000.\n\nExample: 50,000 cm² = 50,000 ÷ 10,000 = 5 m²' },
  { islandId:'mi-17-4', front:'What is the link between cm³ and ml?', back:'1 cm³ = 1 ml (exactly).\n1,000 cm³ = 1 litre.\n1 m³ = 1,000,000 cm³ = 1,000 litres.\n\nSo a cube 10 cm × 10 cm × 10 cm = 1,000 cm³ = 1 litre.' },
  { islandId:'mi-17-4', front:'Which is warmer: −3°C or −8°C?', back:'−3°C is warmer.\n\nOn a number line, −3 is to the RIGHT of −8.\n−8 < −3, so −8°C is colder.\n\nThink: −3°C is only 3 degrees below zero, but −8°C is 8 degrees below.' }
);

// â"€â"€â"€ QUESTIONS â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€

Object.assign(QUESTIONS, {

  // â"€â"€ mi-14-1: Order of Operations: Whole Numbers â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  'mi-14-1': [
    { gen: function() {
        var a = randInt(2, 9), b = randInt(2, 6), c = randInt(2, 8);
        var ans = a + b * c;
        var opts = buildOpts(ans, [(a + b) * c, a * b + c, a + b + c]);
        return { q: 'Evaluate: ' + a + ' + ' + b + ' × ' + c,
                 opts: opts, c: 0,
                 e: 'Multiplication first: ' + b + '×' + c + '=' + (b*c) + '. Then: ' + a + '+' + (b*c) + '=' + ans + '.' };
    }},
    { gen: function() {
        var a = randInt(2, 8), b = randInt(2, 6), d = randInt(2, 4);
        var sum = a + b;
        var ans = sum / d;
        // Keep clean: ensure divisible
        var dOpts = [d * 2, d * 3, sum - 1];
        var opts = buildOpts(ans, [a / d + b, (a + b) * d, a / d]);
        return { q: 'Evaluate: (' + a + ' + ' + b + ') ÷ ' + d,
                 opts: opts, c: 0,
                 e: 'Brackets first: ' + a + '+' + b + '=' + sum + '. Then ' + sum + '÷' + d + '=' + ans + '.' };
    }},
    { gen: function() {
        var base = randInt(2, 5), add = randInt(1, 9);
        var ans = base * base * base + add;
        var opts = buildOpts(ans, [(base + add) * (base + add) * (base + add), base * base + add, base * 3 + add]);
        return { q: 'Evaluate: ' + base + '³ + ' + add,
                 opts: opts, c: 0,
                 e: 'Indices first: ' + base + '³=' + (base*base*base) + '. Then ' + (base*base*base) + '+' + add + '=' + ans + '.' };
    }},
    { gen: function() {
        var a = randInt(10, 25), b = randInt(2, 5), c = randInt(2, 5), d = randInt(1, 4);
        var ans = a - b * c + d;
        var opts = buildOpts(ans, [(a - b) * c + d, a - b * (c + d), a - b * c - d]);
        return { q: 'Evaluate: ' + a + ' − ' + b + ' × ' + c + ' + ' + d,
                 opts: opts, c: 0,
                 e: 'Multiply first: ' + b + '×' + c + '=' + (b*c) + '. Then left to right: ' + a + '−' + (b*c) + '+' + d + '=' + ans + '.' };
    }},
    { gen: function() {
        var a = randInt(2, 6), b = randInt(1, 4), d = randInt(2, 4);
        var inner = a + b;
        var ans = inner * inner / d;
        // Ensure clean result
        var opts = buildOpts(ans, [a * a / d + b, inner * inner * d, (a + b) / d]);
        return { q: 'Evaluate: (' + a + ' + ' + b + ')² ÷ ' + d,
                 opts: opts, c: 0,
                 e: 'Brackets: ' + a + '+' + b + '=' + inner + '. Indices: ' + inner + '²=' + (inner*inner) + '. Divide: ' + (inner*inner) + '÷' + d + '=' + ans + '.' };
    }},
    { gen: function() {
        var m = randInt(2, 5), a = randInt(2, 6), b = randInt(2, 5), exp = randInt(2, 3);
        var ans = m * (a + b) - Math.pow(exp, 2);
        var opts = buildOpts(ans, [m * a + b - exp * exp, m * (a + b) + exp * exp, m * (a + b - exp) * exp]);
        return { q: 'Evaluate: ' + m + ' × (' + a + ' + ' + b + ') − ' + exp + '²',
                 opts: opts, c: 0,
                 e: 'Brackets: ' + a + '+' + b + '=' + (a+b) + '. Indices: ' + exp + '²=' + (exp*exp) + '. Multiply: ' + m + '×' + (a+b) + '=' + (m*(a+b)) + '. Subtract: ' + (m*(a+b)) + '−' + (exp*exp) + '=' + ans + '.' };
    }},
    { gen: function() {
        var a = randInt(20, 100), b = randInt(2, 5), c = randInt(2, 4);
        var ans = a / b / c;
        var opts = buildOpts(ans, [a / (b * c) + b, a * b / c, a / b + c]);
        return { q: 'Evaluate: ' + a + ' ÷ ' + b + ' ÷ ' + c,
                 opts: opts, c: 0,
                 e: 'Left to right: ' + a + '÷' + b + '=' + (a/b) + '. Then ' + (a/b) + '÷' + c + '=' + ans + '.' };
    }},
    // Conceptual: Insert brackets to make true
    { question:'Insert brackets to make this true: 3 + 5 × 2 = 16', options:['(3+5) × 2','3 + (5×2)','(3+5×2)','3 + 5 × (2)'], answer:0, explanation:'(3+5)×2 = 8×2 = 16 ✓' },
    // Conceptual: Which expression equals 7
    { question:'Which expression equals 7? A: 1+2×3 B: (1+2)×3 C: 1+(2×3)', options:['A only','B only','A and C','C only'], answer:2, explanation:'A: 1+6=7 ✓ C: 1+6=7 ✓ B: 3×3=9 ✗' },
    // Conceptual: Student error check
    { question:'A student says 2+3×4−1=19. Are they correct?', options:['Yes','No — answer is 13','No — answer is 15','No — answer is 21'], answer:1, explanation:'3×4=12 first. Then 2+12−1=13.' }
  ],

  // â"€â"€ mi-14-2: Order of Operations: Decimals & Fractions â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  'mi-14-2': [
    { gen: function() {
        var a = pickFrom([0.5, 1.5, 2.5, 0.4, 1.2, 0.8]);
        var b = randInt(2, 6), c = randInt(1, 5);
        var ans = Math.round((a * b + c) * 100) / 100;
        var opts = buildOpts(ans, [Math.round((a + b) * c * 100)/100, Math.round(a * (b + c) * 100)/100, Math.round((a * b - c) * 100)/100]);
        return { q: 'Evaluate: ' + a + ' × ' + b + ' + ' + c,
                 opts: opts, c: 0,
                 e: 'Multiply first: ' + a + '×' + b + '=' + Math.round(a*b*100)/100 + '. Then ' + Math.round(a*b*100)/100 + '+' + c + '=' + ans + '.' };
    }},
    { gen: function() {
        var a = pickFrom([1.5, 2.5, 3.5, 1.2, 2.4]), b = pickFrom([0.5, 1.5, 0.8, 0.6]);
        var d = randInt(2, 4);
        var sum = Math.round((a + b) * 10) / 10;
        var ans = Math.round(sum / d * 100) / 100;
        var opts = buildOpts(ans, [Math.round((a / d + b) * 100)/100, Math.round((a + b * d) * 100)/100, Math.round(a / d * 100)/100]);
        return { q: 'Evaluate: (' + a + ' + ' + b + ') ÷ ' + d,
                 opts: opts, c: 0,
                 e: 'Brackets: ' + a + '+' + b + '=' + sum + '. Then ' + sum + '÷' + d + '=' + ans + '.' };
    }},
    // Conceptual: fraction×integer + fraction×integer
    { question:'Evaluate: ½ × 6 + ¼ × 8', options:['5','3','7','4'], answer:0, explanation:'½×6=3 and ¼×8=2. Then 3+2=5.' },
    { gen: function() {
        var a = pickFrom([1.2, 0.6, 2.4, 1.8, 3.2]);
        var b = pickFrom([0.4, 0.8, 1.5, 2.5]);
        var c = randInt(2, 6);
        var ans = Math.round((a + b * c) * 100) / 100;
        var opts = buildOpts(ans, [Math.round((a + b) * c * 100)/100, Math.round(a * b + c * 100)/100, Math.round((a - b * c) * 100)/100]);
        return { q: 'Evaluate: ' + a + ' + ' + b + ' × ' + c,
                 opts: opts, c: 0,
                 e: 'Multiply: ' + b + '×' + c + '=' + Math.round(b*c*100)/100 + '. Then ' + a + '+' + Math.round(b*c*100)/100 + '=' + ans + '.' };
    }},
    // Conceptual: fraction brackets squared × integer
    { question:'Evaluate: (¾ + ¼)² × 4', options:['4','8','1','16'], answer:0, explanation:'Brackets: ¾+¼=1. Indices: 1²=1. Multiply: 1×4=4.' },
    { gen: function() {
        var pairs = [[3.6, 0.9, 2], [4.8, 1.2, 2], [2.4, 0.6, 2], [7.2, 1.8, 2], [6.4, 0.8, 2]];
        var p = pickFrom(pairs);
        var num = p[0], fac1 = p[1], fac2 = p[2];
        var denom = Math.round(fac1 * fac2 * 100) / 100;
        var ans = Math.round(num / denom * 100) / 100;
        var opts = buildOpts(ans, [Math.round(num / fac1 * 100)/100, Math.round(num * denom * 100)/100, Math.round(num / fac2 * 100)/100]);
        return { q: 'Evaluate: ' + num + ' ÷ (' + fac1 + ' × ' + fac2 + ')',
                 opts: opts, c: 0,
                 e: 'Brackets: ' + fac1 + '×' + fac2 + '=' + denom + '. Then ' + num + '÷' + denom + '=' + ans + '.' };
    }},
    // Conceptual: fraction × integer subtraction giving 0
    { question:'Evaluate: 2 − ⅓ × 6', options:['0','5⅓','4','1'], answer:0, explanation:'Multiply: ⅓×6=2. Then 2−2=0.' },
    { gen: function() {
        var b = pickFrom([0.2, 0.3, 0.4, 0.5]);
        var bSq = Math.round(b * b * 100) / 100;
        var ans = Math.round(bSq * 2 * 100) / 100;
        var opts = buildOpts(ans, [Math.round((b * 2) * (b * 2) * 100)/100, Math.round(b * 2 * 100)/100, Math.round(bSq + b * 100)/100]);
        return { q: 'Evaluate: (' + b + ')² + ' + bSq,
                 opts: opts, c: 0,
                 e: 'Indices: (' + b + ')²=' + bSq + '. Then ' + bSq + '+' + bSq + '=' + ans + '.' };
    }},
    // Conceptual: place brackets to get target value
    { question:'Place brackets to get 2: ¼ + ¾ × 2', options:['(¼+¾) × 2','¼ + (¾×2)','Neither','Both give 2'], answer:0, explanation:'(¼+¾)×2=1×2=2 ✓. ¼+(¾×2)=¼+1.5=1.75 ✗.' },
    { gen: function() {
        var m = randInt(2, 6);
        var a = pickFrom([1.2, 1.5, 1.8, 2.4]);
        var sub = pickFrom([0.2, 0.4, 0.5, 0.8]);
        var inner = Math.round((a - sub) * 10) / 10;
        var innerSq = Math.round(inner * inner * 100) / 100;
        var add = randInt(1, 5);
        var ans = Math.round((m * innerSq + add) * 100) / 100;
        var opts = buildOpts(ans, [Math.round((m * inner + add) * 100)/100, Math.round((m + innerSq + add) * 100)/100, Math.round((m * a * innerSq + add) * 100)/100]);
        return { q: 'Evaluate: ' + m + ' × (' + a + ' − ' + sub + ')² + ' + add,
                 opts: opts, c: 0,
                 e: 'Brackets: ' + a + '−' + sub + '=' + inner + '. Indices: ' + inner + '²=' + innerSq + '. Multiply: ' + m + '×' + innerSq + '=' + Math.round(m*innerSq*100)/100 + '. Add: ' + Math.round(m*innerSq*100)/100 + '+' + add + '=' + ans + '.' };
    }}
  ],

  // â"€â"€ mi-14-3: Order of Operations: Integers â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  'mi-14-3': [
    { gen: function() {
        var a = randInt(2, 6), b = randInt(2, 5), c = randInt(1, 8);
        var ans = -a * b + c;
        var opts = buildOpts(ans, [(-a + c) * b, -(a * b + c), -a * (b + c)]);
        return { q: 'Evaluate: −' + a + ' × ' + b + ' + ' + c,
                 opts: opts, c: 0,
                 e: 'Multiply: −' + a + '×' + b + '=−' + (a*b) + '. Then −' + (a*b) + '+' + c + '=' + ans + '.' };
    }},
    { gen: function() {
        var base = randInt(2, 4);
        var ans = -(base * base * base);
        var opts = buildOpts(ans, [base * base * base, -(base * 3), base * base]);
        return { q: 'Evaluate: (−' + base + ')³',
                 opts: opts, c: 0,
                 e: '(−' + base + ')³=(−' + base + ')×(−' + base + ')×(−' + base + ')=' + (base*base) + '×(−' + base + ')=' + ans + '.' };
    }},
    { gen: function() {
        var a = randInt(5, 15), b = randInt(2, 5), c = randInt(2, 6);
        var ans = a - (-b) * c;
        var opts = buildOpts(ans, [a + b * c - 2 * b * c, a - b * c, (a - b) * c]);
        return { q: 'Evaluate: ' + a + ' − (−' + b + ') × ' + c,
                 opts: opts, c: 0,
                 e: 'Multiply: (−' + b + ')×' + c + '=−' + (b*c) + '. Then ' + a + '−(−' + (b*c) + ')=' + a + '+' + (b*c) + '=' + ans + '.' };
    }},
    { gen: function() {
        var n = randInt(2, 7), add = randInt(1, 8);
        var ans = -(n * n) + add;
        var opts = buildOpts(ans, [n * n + add, -(n + add) * (n + add), -(n * 2) + add]);
        return { q: 'Evaluate: −' + n + '² + ' + add,
                 opts: opts, c: 0,
                 e: 'Indices: ' + n + '²=' + (n*n) + '. So −' + n + '²=−' + (n*n) + '. Then −' + (n*n) + '+' + add + '=' + ans + '. (Note: −' + n + '² means −(' + n + '²), not (−' + n + ')².)' };
    }},
    { gen: function() {
        var n = randInt(2, 7), add = randInt(1, 8);
        var ans = n * n + add;
        var opts = buildOpts(ans, [-(n * n) + add, n * n - add, -n * n - add]);
        return { q: 'Evaluate: (−' + n + ')² + ' + add,
                 opts: opts, c: 0,
                 e: '(−' + n + ')²=' + (n*n) + '. Then ' + (n*n) + '+' + add + '=' + ans + '.' };
    }},
    { gen: function() {
        var m = randInt(2, 5), a = randInt(2, 6), b = randInt(3, 9);
        var inner = a - b;
        var innerSq = inner * inner;
        var ans = -m * innerSq;
        var opts = buildOpts(ans, [m * innerSq, -m * (a - b), -(m + innerSq)]);
        return { q: 'Evaluate: −' + m + ' × (' + a + ' − ' + b + ')²',
                 opts: opts, c: 0,
                 e: 'Brackets: ' + a + '−' + b + '=' + inner + '. Indices: (' + inner + ')²=' + innerSq + '. Multiply: −' + m + '×' + innerSq + '=' + ans + '.' };
    }},
    { gen: function() {
        var a = randInt(12, 30), b = randInt(2, 5), c = randInt(2, 6);
        var ans = a / (-b) - (-c);
        var ans2 = Math.round(ans * 100) / 100;
        var opts = buildOpts(ans2, [Math.round((a / (-b) + c - 2) * 100)/100, Math.round(a / b - c * 100)/100, Math.round((-a / b) - c * 100)/100]);
        return { q: 'Evaluate: ' + a + ' ÷ (−' + b + ') − (−' + c + ')',
                 opts: opts, c: 0,
                 e: 'Divide: ' + a + '÷(−' + b + ')=−' + (a/b) + '. Then −' + (a/b) + '−(−' + c + ')=−' + (a/b) + '+' + c + '=' + ans2 + '.' };
    }},
    // Conceptual: sign rules for negative squared
    { question:'Which is correct? A: −5² = 25 B: (−5)² = 25', options:['A only','B only','Both','Neither'], answer:1, explanation:'−5²=−25 (negative of 25). (−5)²=+25. Only B is 25.' },
    { gen: function() {
        var a = randInt(2, 6), b = randInt(2, 5), m = randInt(2, 5), d = randInt(2, 4);
        var inner = -a + (-b);
        var step1 = inner * (-m);
        var ans = step1 / d;
        var opts = buildOpts(ans, [inner * m / d, step1 * d, inner / d]);
        return { q: 'Evaluate: [−' + a + ' + (−' + b + ')] × −' + m + ' ÷ ' + d,
                 opts: opts, c: 0,
                 e: 'Brackets: −' + a + '+(−' + b + ')=−' + (a+b) + '. Multiply: −' + (a+b) + '×(−' + m + ')=' + step1 + '. Divide: ' + step1 + '÷' + d + '=' + ans + '.' };
    }},
    { gen: function() {
        var base = randInt(2, 4);
        var pow4 = base * base * base * base;
        var pow3 = base * base * base;
        var ans = pow4 - pow3;
        var opts = buildOpts(ans, [pow4 + pow3, pow4 - pow3 - 2 * pow3, pow3 - pow4]);
        return { q: 'The expression (−' + base + ')⁴ − (−' + base + ')³ equals:',
                 opts: opts, c: 0,
                 e: '(−' + base + ')⁴=' + pow4 + '. (−' + base + ')³=−' + pow3 + '. ' + pow4 + '−(−' + pow3 + ')=' + pow4 + '+' + pow3 + '=' + ans + '.' };
    }}
  ],

  // â"€â"€ mi-15-1: Rational Numbers on Number Lines â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  'mi-15-1': [
    // Conceptual: definition of rational number
    { question:'Which of these is a rational number?', options:['π','√2','3/7','√3'], answer:2, explanation:'3/7 is rational (p/q form). π and √2 are irrational.' },
    // Conceptual: location of negative half
    { question:'Where is −½ on a number line?', options:['Between −1 and 0','Between 0 and 1','At −2','To the right of 0'], answer:0, explanation:'−½ is negative so left of 0, halfway between 0 and −1.' },
    // Conceptual: which rational is between 0 and 1/2
    { question:'Which rational number is between 0 and ½?', options:['¾','⅛','⅔','−¼'], answer:1, explanation:'⅛=0.125, which is between 0 and 0.5.' },
    // Conceptual: convert recurring decimal to fraction
    { question:'Convert 0.6̅ (0.6 recurring) to a fraction.', options:['6/10','2/3','3/5','5/8'], answer:1, explanation:'Let x=0.666… 10x=6.666… 10x−x=6, 9x=6, x=6/9=2/3.' },
    { gen: function() {
        var fracs = [[5,8,0.625],[3,8,0.375],[7,8,0.875],[1,4,0.25],[3,4,0.75],[1,8,0.125]];
        var f = pickFrom(fracs);
        var num = f[0], den = f[1], dec = f[2];
        var opts = buildOpts(dec, [num/10, Math.round((num/den + 0.1)*1000)/1000, Math.round((num+1)/den*1000)/1000]);
        return { q: 'Which decimal is equivalent to ' + num + '/' + den + '?',
                 opts: opts, c: 0,
                 e: num + '÷' + den + '=' + dec + '.' };
    }},
    // Conceptual: which is furthest left on number line
    { question:'Which number is furthest left on a number line?', options:['−⅓','−¾','−½','−¼'], answer:1, explanation:'All negative; most negative (furthest left) = −¾ = −0.75.' },
    // Conceptual: recurring decimal is rational
    { question:'A rational number has the decimal 0.142857142857… Is this rational?', options:['Yes — it is recurring','No — it is irrational','Cannot tell','Only if it terminates'], answer:0, explanation:'Recurring decimals are always rational. This is 1/7.' },
    // Conceptual: ordering on number line
    { question:'Plot these in order: −⅖, 0.5, −0.2, ¾. Which comes second?', options:['0.5','−0.2','−⅖','¾'], answer:1, explanation:'Order: −0.6, −0.2, 0.5, 0.75. Second is −0.2.' },
    { gen: function() {
        var fracs = [[3,8,0.375],[1,4,0.25],[5,8,0.625],[7,8,0.875],[1,8,0.125]];
        var f = pickFrom(fracs);
        var num = f[0], den = f[1], dec = f[2];
        var altDen = den + 1;
        var opts = buildOpts(num + '/' + den, [(num+1) + '/' + den, num + '/' + (den+1), num + '/' + (den-1)]);
        return { q: 'Which fraction is equivalent to ' + dec + '?',
                 opts: opts, c: 0,
                 e: dec + ' = ' + (dec * 1000) + '/1000 = ' + num + '/' + den + '.' };
    }},
    { gen: function() {
        var numer = randInt(3, 13), denom = randInt(3, 6);
        // ensure not whole number
        while (numer % denom === 0) numer = randInt(3, 13);
        var val = -numer / denom;
        var floor = Math.floor(val);
        var ceil = floor + 1;
        var opts = buildOpts(floor + ' and ' + ceil, [(floor-1) + ' and ' + floor, ceil + ' and ' + (ceil+1), (floor-2) + ' and ' + (floor-1)]);
        return { q: 'Between which two consecutive integers does −' + numer + '/' + denom + ' lie?',
                 opts: opts, c: 0,
                 e: '−' + numer + '/' + denom + ' ≈ ' + Math.round(val*100)/100 + ', which lies between ' + floor + ' and ' + ceil + '.' };
    }}
  ],

  // â"€â"€ mi-15-2: Compare & Order Rational Numbers â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  'mi-15-2': [
    // Conceptual: compare two negative fractions
    { question:'Which is greater: −¼ or −⅓?', options:['−¼','−⅓','They are equal','Cannot compare'], answer:0, explanation:'−¼=−0.25, −⅓≈−0.333. −0.25 > −0.333, so −¼ is greater.' },
    // Conceptual: order fraction, decimal, percent
    { question:'Order smallest to largest: ⅓, 0.6, 60%', options:['60%, ⅓, 0.6','0.6, 60%, ⅓','60%=0.6, then ⅓','All equal'], answer:2, explanation:'60%=0.6=⅖. ⅓≈0.667. So 0.6=60% < ⅓.' },
    // Conceptual: compare two negative decimals/fractions
    { question:'Which is larger: −2.5 or −12/5?', options:['−2.5','−12/5','They are equal','Cannot tell'], answer:1, explanation:'−12/5 = −2.4. Since −2.4 > −2.5 on the number line, −12/5 is the larger value.' },
    // Conceptual: which fraction closest to 1
    { question:'Which fraction is closest to 1?', options:['⅓','¾','⅘','⅚'], answer:3, explanation:'⅚≈0.833, ⅘=0.8, ¾=0.75, ⅓≈0.667. ⅚ is closest to 1.' },
    // Conceptual: every integer is rational
    { question:'True or false: Every integer is a rational number.', options:['True','False','Sometimes','Only positive integers'], answer:0, explanation:'Any integer n = n/1, which is p/q form. All integers are rational.' },
    // Conceptual: correct ordering of set
    { question:'Which set is ordered correctly (smallest to largest)?', options:['−⅓, −½, 0, ¼','−½, −⅓, 0, ¼','¼, 0, −⅓, −½','0, −⅓, −½, ¼'], answer:1, explanation:'−½=−0.5, −⅓≈−0.333, 0, ¼=0.25. Correct order: −½,−⅓,0,¼.' },
    { gen: function() {
        var fracs = [[7,4],[5,3],[9,4],[11,5],[8,3]];
        var f = pickFrom(fracs);
        var num = f[0], den = f[1];
        var ans = num + '/' + den;
        var opts = buildOpts(ans, [den + '/' + num, '-' + num + '/' + den, num + '/' + (den+1)]);
        return { q: 'What is the absolute value of −' + num + '/' + den + '?',
                 opts: opts, c: 0,
                 e: '|−' + num + '/' + den + '| = ' + num + '/' + den + ' (distance from zero is always positive).' };
    }},
    // Conceptual: midpoint of two fractions
    { question:'Which rational number is exactly halfway between ⅓ and ⅔?', options:['½','⅓','¼','⅕'], answer:0, explanation:'Midpoint = (⅓+⅔)/2 = 1/2.' },
    // Conceptual: closest to 0
    { question:'Which is closest to 0 on a number line?', options:['−3/10','−2/5','2/3','−4/5'], answer:0, explanation:'Convert: −0.3, −0.4, 0.667, −0.8. Closest to 0 is −0.3 = −3/10.' },
    // Conceptual: temperature comparison
    { question:'A thermometer shows −4½Â°C and −4.3°C. Which is colder?', options:['−4½Â°C','−4.3°C','Same temperature','Cannot compare'], answer:0, explanation:'−4½=−4.5 and −4.3. −4.5 < −4.3, so −4½Â°C is colder.' }
  ],

  // â"€â"€ mi-15-3: Rational Number Problems â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  'mi-15-3': [
    { gen: function() {
        var start = randInt(1, 8);
        var dropFrac = pickFrom([0.5, 1.5, 2.5, 3.5]);
        var endTemp = -(dropFrac);
        var change = Math.round((endTemp - start) * 10) / 10;
        var ans = change + '°C';
        var opts = buildOpts(ans, [(start + dropFrac) + '°C', Math.round((endTemp + start) * 10)/10 + '°C', Math.round((start - dropFrac)*10)/10 + '°C']);
        return { q: 'The temperature falls from ' + start + '°C to −' + dropFrac + '°C. What is the change?',
                 opts: opts, c: 0,
                 e: 'Change = −' + dropFrac + ' − ' + start + ' = ' + change + '°C (a drop of ' + Math.abs(change) + ' degrees).' };
    }},
    { gen: function() {
        var depth = pickFrom([8.4, 10.2, 12.6, 9.5, 7.8]);
        var rise = pickFrom([2.4, 3.6, 1.5, 4.2]);
        var ans = Math.round((-depth + rise) * 100) / 100;
        var opts = buildOpts(ans + 'm', [Math.round(-(depth + rise)*100)/100 + 'm', Math.round((depth - rise)*100)/100 + 'm', Math.round((depth + rise)*100)/100 + 'm']);
        return { q: 'A diver is at −' + depth + 'm. She rises ' + rise + 'm. What is her new depth?',
                 opts: opts, c: 0,
                 e: '−' + depth + ' + ' + rise + ' = ' + ans + 'm.' };
    }},
    { gen: function() {
        var scores = [randInt(-4, -1), randInt(1, 4), randInt(-3, -1), 0];
        var total = scores[0] + scores[1] + scores[2] + scores[3];
        var opts = buildOpts(total, [total + 1, total - 1, -total]);
        return { q: 'A golfer scores ' + scores[0] + ', +' + scores[1] + ', ' + scores[2] + ', ' + scores[3] + ' over four rounds. What is the total?',
                 opts: opts, c: 0,
                 e: scores[0] + '+' + scores[1] + '+(' + scores[2] + ')+' + scores[3] + ' = ' + total + '.' };
    }},
    { gen: function() {
        // -a/4 + b/4 style addition with common denominator 4
        var a = randInt(1, 7), b = randInt(1, a);
        var numAns = b - a;
        var den = 4;
        // simplify
        var g = (function gcd(x,y){return y?gcd(y,x%y):x;})(Math.abs(numAns), den);
        var ansStr = (numAns === 0) ? '0' : (numAns/g) + '/' + (den/g);
        var opts = buildOpts(ansStr, [(a-b) + '/' + den, '−' + (a+b) + '/' + den, (a+b) + '/' + den]);
        return { q: 'What is −' + a + '/' + den + ' + ' + b + '/' + den + '?',
                 opts: opts, c: 0,
                 e: '−' + a + '/' + den + ' + ' + b + '/' + den + ' = ' + numAns + '/' + den + (g > 1 ? ' = ' + ansStr : '') + '.' };
    }},
    // Conceptual: which is NOT rational
    { question:'Which is NOT a rational number?', options:['0.25','√5','−3','7/8'], answer:1, explanation:'√5 is irrational — it cannot be expressed as p/q.' },
    { gen: function() {
        var debt = pickFrom([45.50, 32.75, 18.25, 56.00, 27.50]);
        var deposit = randInt(50, 200);
        var ans = Math.round((deposit - debt) * 100) / 100;
        var opts = buildOpts('£' + ans.toFixed(2), ['−£' + ans.toFixed(2), '£' + (deposit + debt).toFixed(2), '£' + (deposit - debt - 10).toFixed(2)]);
        return { q: 'A bank account has −£' + debt.toFixed(2) + '. After a deposit of £' + deposit + ', what is the balance?',
                 opts: opts, c: 0,
                 e: '−' + debt.toFixed(2) + ' + ' + deposit + ' = £' + ans.toFixed(2) + '.' };
    }},
    // Conceptual: product of two negatives
    { question:'Is the product of two negative rational numbers positive or negative?', options:['Positive','Negative','Zero','Cannot tell'], answer:0, explanation:'Negative × Negative = Positive (same sign rule).' },
    // Conceptual: (-2/3)^2
    { question:'What is (−⅔)²?', options:['4/9','−4/9','−⅔','4/6'], answer:0, explanation:'(−⅔)² = (−⅔)×(−⅔) = 4/9. Same signs → positive.' },
    { gen: function() {
        var descend = pickFrom([12.5, 15.0, 10.5, 18.0, 8.5]);
        var rise = pickFrom([3.25, 4.5, 2.75, 5.0]);
        var ans = Math.round((-descend + rise) * 100) / 100;
        var opts = buildOpts(ans + 'm', [Math.round(-(descend + rise)*100)/100 + 'm', Math.round((descend - rise)*100)/100 + 'm', Math.round((ans - 1)*100)/100 + 'm']);
        return { q: 'A submarine descends ' + descend + 'm below sea level, then rises ' + rise + 'm. What is its position?',
                 opts: opts, c: 0,
                 e: '−' + descend + ' + ' + rise + ' = ' + ans + 'm (' + Math.abs(ans) + 'm below sea level).' };
    }},
    // Conceptual: |−5| vs −|5| vs −(−5)
    { question:'Which expression has the greatest value? A: |−5| B: −|5| C: −(−5)', options:['A only','A and C equal, both greatest','B','C only'], answer:1, explanation:'|−5|=5. −|5|=−5. −(−5)=5. A and C both equal 5, which is greatest.' }
  ],

  // â"€â"€ mi-16-1: Multi-Step Word Problems â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  'mi-16-1': [
    { gen: function() {
        var total = randInt(20, 60) * 12;
        var n = randInt(30, 80);
        var pricePence = total / n;
        // keep clean: pick n that divides total
        var items = randInt(3, 9);
        var ans = Math.round(pricePence * items * 100) / 100;
        var perItem = Math.round(pricePence * 100) / 100;
        var totalPounds = Math.round(total / 100 * 100) / 100;
        var opts = buildOpts('£' + ans.toFixed(2), ['£' + Math.round(pricePence * (items+1) * 100)/100, '£' + Math.round(pricePence * items / 2 * 100)/100, '£' + Math.round(perItem * 100)/100]);
        return { q: 'A box of ' + n + ' chocolates costs £' + totalPounds.toFixed(2) + '. How much do ' + items + ' chocolates cost?',
                 opts: opts, c: 0,
                 e: 'Cost per chocolate = £' + totalPounds.toFixed(2) + '÷' + n + '=£' + perItem.toFixed(2) + '. ' + items + '×£' + perItem.toFixed(2) + '=£' + ans.toFixed(2) + '.' };
    }},
    { gen: function() {
        var diff = randInt(2, 6);
        var total = randInt(14, 28);
        // sam + (sam+diff) = total => 2*sam = total - diff
        while ((total - diff) % 2 !== 0) total++;
        var sam = (total - diff) / 2;
        var jan = sam + diff;
        var opts = buildOpts(sam + ' km/h', [(total / 2) + ' km/h', (sam - 1) + ' km/h', jan + ' km/h']);
        return { q: 'Jan runs ' + diff + ' km/h faster than Sam. Together they run ' + total + ' km/h. How fast is Sam?',
                 opts: opts, c: 0,
                 e: 'Sam+(Sam+' + diff + ')=' + total + '. 2×Sam=' + (total-diff) + '. Sam=' + sam + ' km/h.' };
    }},
    { gen: function() {
        var arrHH = randInt(12, 18), arrMM = randInt(10, 55);
        var durH = randInt(1, 3), durM = randInt(10, 55);
        var totalMins = arrHH * 60 + arrMM - durH * 60 - durM;
        var depH = Math.floor(totalMins / 60);
        var depM = totalMins % 60;
        var arrStr = arrHH + ':' + (arrMM < 10 ? '0' : '') + arrMM;
        var depStr = depH + ':' + (depM < 10 ? '0' : '') + depM;
        var wrong1H = depH - 1, wrong1M = depM;
        var wrong2H = depH, wrong2M = (depM + 10) % 60;
        var wrong1 = wrong1H + ':' + (wrong1M < 10 ? '0' : '') + wrong1M;
        var wrong2 = wrong2H + ':' + (wrong2M < 10 ? '0' : '') + wrong2M;
        var wrong3H = depH + 1, wrong3M = depM;
        var wrong3 = wrong3H + ':' + (wrong3M < 10 ? '0' : '') + wrong3M;
        var opts = buildOpts(depStr, [wrong1, wrong2, wrong3]);
        return { q: 'A train journey takes ' + durH + 'h ' + durM + 'min. It arrives at ' + arrStr + '. What time did it depart?',
                 opts: opts, c: 0,
                 e: arrStr + ' − ' + durH + 'h=' + (arrHH - durH) + ':' + (arrMM < 10 ? '0' : '') + arrMM + ', then −' + durM + 'min=' + depStr + '.' };
    }},
    { gen: function() {
        var pct1 = pickFrom([20, 25, 40, 50]);
        var val1 = randInt(2, 8) * (100 / pct1);
        var pct2 = pickFrom([25, 50, 75]);
        var full = val1;
        var ans = Math.round(full * pct2 / 100 * 100) / 100;
        var opts = buildOpts(ans, [Math.round(full * pct2 / 100 + 10), Math.round(val1 * 2 * 100)/100, Math.round(full * (pct1 + pct2) / 100 * 100)/100]);
        return { q: pct1 + '% of a number is ' + val1 + '. What is ' + pct2 + '% of the same number?',
                 opts: opts, c: 0,
                 e: '100% = ' + val1 + '÷' + (pct1/100) + '=' + full + '. ' + pct2 + '% of ' + full + ' = ' + ans + '.' };
    }},
    { gen: function() {
        var adultP = randInt(5, 12), childP = randInt(3, adultP - 1);
        var nAdults = randInt(1, 4), nChildren = randInt(1, 5);
        var ans = nAdults * adultP + nChildren * childP;
        var opts = buildOpts('£' + ans, ['£' + (nAdults * adultP + nChildren * (childP + 1)), '£' + ((nAdults + nChildren) * adultP), '£' + (nAdults * (adultP - 1) + nChildren * childP)]);
        return { q: 'Tickets cost £' + adultP + ' adults, £' + childP + ' children. A family of ' + nAdults + ' adult' + (nAdults>1?'s':'') + ' and ' + nChildren + ' child' + (nChildren>1?'ren':'') + '. Total cost?',
                 opts: opts, c: 0,
                 e: nAdults + '×£' + adultP + ' + ' + nChildren + '×£' + childP + ' = £' + (nAdults*adultP) + '+£' + (nChildren*childP) + ' = £' + ans + '.' };
    }},
    // Conceptual: successive percentage reductions
    { question:'A shop reduces prices by 30%, then by a further 20%. What is the overall percentage reduction?', options:['44%','50%','42%','40%'], answer:0, explanation:'×0.7 then ×0.8 = ×0.56. Reduction = 1−0.56 = 44%.' },
    { gen: function() {
        var workers = randInt(2, 5), days = randInt(4, 10);
        var totalWork = workers * days;
        var newWorkers = workers + randInt(1, 3);
        var ans = totalWork / newWorkers;
        var opts = buildOpts(ans + ' days', [(workers * days + newWorkers) + ' days', (days + newWorkers) + ' days', Math.round(totalWork / (newWorkers - 1)) + ' days']);
        return { q: workers + ' workers take ' + days + ' days to build a wall. How long for ' + newWorkers + ' workers?',
                 opts: opts, c: 0,
                 e: 'Inverse proportion: ' + workers + '×' + days + '=' + totalWork + ' total work. ' + newWorkers + ' workers: ' + totalWork + '÷' + newWorkers + '=' + ans + ' days.' };
    }},
    { gen: function() {
        var w = randInt(4, 10), l = w + randInt(2, 6);
        var perim = 2 * (l + w);
        var area = l * w;
        var diff = l - w;
        var opts = buildOpts(area + 'cm²', [(l + 1) * w + 'cm²', (l - 1) * (w + 1) + 'cm²', (l + w) + 'cm²']);
        return { q: 'A rectangle has perimeter ' + perim + 'cm. Its length is ' + diff + 'cm more than its width. Find the area.',
                 opts: opts, c: 0,
                 e: '2(l+w)=' + perim + ' → l+w=' + (l+w) + '. l=w+' + diff + ' → 2w+' + diff + '=' + (l+w) + ' → w=' + w + ', l=' + l + '. Area = ' + w + '×' + l + ' = ' + area + 'cm².' };
    }},
    { gen: function() {
        var s1 = pickFrom([40, 50, 60, 70, 80]), t1n = randInt(2, 4), t1d = pickFrom([1, 2]);
        var t1 = t1n + (t1d === 2 ? 0.5 : 0);
        var s2 = pickFrom([30, 40, 50, 60]), t2n = randInt(1, 3);
        var d1 = s1 * t1, d2 = s2 * t2n;
        var ans = d1 + d2;
        var t1str = t1d === 2 ? t1n + '.5' : '' + t1n;
        var opts = buildOpts(ans + ' miles', [(d1 + d2 + 10) + ' miles', (d1 - d2) + ' miles', Math.round(ans * 1.1) + ' miles']);
        return { q: 'A car travels at ' + s1 + 'mph for ' + t1str + ' hours, then at ' + s2 + 'mph for ' + t2n + ' hours. Total distance?',
                 opts: opts, c: 0,
                 e: s1 + '×' + t1str + '=' + d1 + '. ' + s2 + '×' + t2n + '=' + d2 + '. Total=' + ans + ' miles.' };
    }},
    { gen: function() {
        var n = randInt(4, 7);
        var mean = randInt(8, 16);
        var known = [];
        for (var i = 0; i < n - 1; i++) known.push(randInt(mean - 5, mean + 5));
        var knownSum = known.reduce(function(a, b){ return a + b; }, 0);
        var fifth = n * mean - knownSum;
        var opts = buildOpts(fifth, [fifth + 2, fifth - 3, fifth + 5]);
        return { q: 'The mean of ' + n + ' numbers is ' + mean + '. ' + (n-1) + ' of them are ' + known.join(', ') + '. Find the ' + n + 'th number.',
                 opts: opts, c: 0,
                 e: 'Total=' + n + '×' + mean + '=' + (n*mean) + '. Sum of ' + (n-1) + '=' + knownSum + '. ' + n + 'th=' + (n*mean) + '−' + knownSum + '=' + fifth + '.' };
    }}
  ],

  // â"€â"€ mi-16-2: Estimation Strategies â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  'mi-16-2': [
    { gen: function() {
        var a = randInt(3, 9) * 10 + randInt(1, 9);
        var b = randInt(1, 4) * 10 + randInt(1, 9);
        var aR = Math.round(a / Math.pow(10, Math.floor(Math.log10(a)))) * Math.pow(10, Math.floor(Math.log10(a)));
        var bR = Math.round(b / Math.pow(10, Math.floor(Math.log10(b)))) * Math.pow(10, Math.floor(Math.log10(b)));
        var est = aR * bR;
        var opts = buildOpts(est, [est + 200, est - 100, aR * b]);
        return { q: 'Estimate ' + a + ' × ' + b + ' by rounding to 1 s.f.',
                 opts: opts, c: 0,
                 e: aR + ' × ' + bR + ' = ' + est + '. (Actual: ' + (a*b) + ')' };
    }},
    { gen: function() {
        var a = randInt(200, 999), b = randInt(10, 49);
        var aR = Math.round(a / 100) * 100;
        var bR = Math.round(b / 10) * 10;
        var est = Math.round(aR / bR);
        var actual = Math.round(a / b * 10) / 10;
        var opts = buildOpts(est, [est + 5, est - 5, est * 2]);
        return { q: 'Estimate ' + a + ' ÷ ' + b + ' to the nearest whole number.',
                 opts: opts, c: 0,
                 e: aR + ' ÷ ' + bR + ' = ' + est + '. (Actual: ≈' + actual + ')' };
    }},
    { gen: function() {
        var ones = randInt(1, 9), rest = randInt(100, 999);
        var n = ones * 1000 + rest;
        var roundedUp = (ones + (rest >= 500 ? 1 : 0)) * 1000;
        var rounded = Math.round(n / 1000) * 1000;
        var opts = buildOpts(rounded, [ones * 1000, Math.round(n / 100) * 100, Math.round(n / 10) * 10]);
        return { q: 'Round ' + n.toLocaleString() + ' to 1 significant figure.',
                 opts: opts, c: 0,
                 e: 'First s.f. = ' + ones + ' (thousands). Next digit ' + Math.floor(rest/100) + (Math.floor(rest/100) >= 5 ? ' ≥ 5 → round up: ' : ' < 5 → round down: ') + rounded.toLocaleString() + '.' };
    }},
    // Conceptual: check calculator reasonableness
    { question:'A calculator gives 13.68 for 3.8 × 3.6. Is this reasonable?', options:['Yes — estimate: 4×4=16','No — answer should be 30+','No — answer should be less than 10','Yes, because both numbers are positive'], answer:0, explanation:'≈4×4=16. 13.68 is close to 16 and reasonable.' },
    { gen: function() {
        var n = randInt(25, 80);
        var nSq = n * n;
        var sqrtEst = Math.round(Math.sqrt(nSq));
        var opts = buildOpts('≈' + sqrtEst, ['≈' + (sqrtEst + 2), '≈' + (sqrtEst - 2), '≈' + (sqrtEst + 5)]);
        return { q: 'Estimate: √(' + nSq + ')',
                 opts: opts, c: 0,
                 e: sqrtEst + '²=' + nSq + '. So √' + nSq + ' = ' + sqrtEst + ' (exact).' };
    }},
    { gen: function() {
        var a = (randInt(30, 90) / 10), b = (randInt(20, 80) / 10);
        var aR = Math.round(a), bR = Math.round(b);
        var est = aR * bR;
        var actual = Math.round(a * b * 100) / 100;
        var opts = buildOpts(est, [est + 10, est - 5, aR * bR + aR]);
        return { q: 'The answer to ' + a + ' × ' + b + ' should be approximately:',
                 opts: opts, c: 0,
                 e: aR + '×' + bR + '=' + est + '. (Actual: ' + actual + ')' };
    }},
    { gen: function() {
        var base = randInt(5, 12);
        var n = base - (Math.random() > 0.5 ? 0.1 : 0.03);
        n = Math.round(n * 100) / 100;
        var baseEst = base * base;
        var opts = buildOpts(baseEst, [(base-1)*(base-1), (base+1)*(base+1), baseEst - 5]);
        return { q: 'Which estimate is closest to the actual value of ' + n + '²?',
                 opts: opts, c: 0,
                 e: base + '²=' + baseEst + '. ' + n + '²≈' + Math.round(n*n*10)/10 + '. ' + baseEst + ' is the closest estimate.' };
    }},
    { gen: function() {
        var students = randInt(300, 900) + randInt(1, 50);
        var busSize = pickFrom([40, 45, 50, 52, 55]);
        var sR = Math.round(students / 100) * 100;
        var bR = Math.round(busSize / 10) * 10;
        var est = Math.ceil(sR / bR);
        var actual = Math.ceil(students / busSize);
        var opts = buildOpts(est, [est + 4, est - 2, Math.floor(students / busSize)]);
        return { q: 'A school has ' + students + ' students. About how many buses of ' + busSize + ' are needed to transport them all?',
                 opts: opts, c: 0,
                 e: sR + '÷' + bR + '=' + (sR/bR) + '. You would need ' + est + ' buses (actual: ' + Math.round(students/busSize*10)/10 + ', so ' + actual + ' needed).' };
    }},
    { gen: function() {
        var a = randInt(2, 5) * 100 + randInt(1, 9);
        var b = randInt(1, 3) * 100 + randInt(1, 99);
        var aR = Math.round(a / 100) * 100;
        var bR = Math.round(b / 100) * 100;
        var est = aR * bR;
        var actual = a * b;
        var opts = buildOpts(est, [est * 2, Math.round(est / 2), est + 10000]);
        return { q: 'Without calculating exactly, which is the best estimate for ' + a + ' × ' + b + '?',
                 opts: opts, c: 0,
                 e: aR + '×' + bR + '=' + est.toLocaleString() + '. (Actual: ' + actual.toLocaleString() + ')' };
    }},
    // Conceptual: evaluate a student's estimation strategy
    { question:'A student estimates 2,950 ÷ 47 ≈ 60. Is this a good estimate and why?', options:['Yes — 3000÷50=60','No — should use 2900÷47','Yes — but only if both rounded up','No — answer is much larger'], answer:0, explanation:'Rounding 2950→3000 and 47→50 gives 3000÷50=60. Good estimate (actual≈62.8).' }
  ],

  // â"€â"€ mi-16-3: Logical Reasoning Problems â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  'mi-16-3': [
    { gen: function() {
        var start = randInt(2, 12);
        var mult = randInt(2, 5);
        var sub = randInt(1, 8);
        var result = start * mult - sub;
        var opts = buildOpts(start, [start + 1, start - 1, start + 2]);
        return { q: 'I think of a number, multiply by ' + mult + ', subtract ' + sub + ', and get ' + result + '. What is my number?',
                 opts: opts, c: 0,
                 e: 'Reverse: ' + result + '+' + sub + '=' + (result+sub) + ', ' + (result+sub) + '÷' + mult + '=' + start + '.' };
    }},
    { gen: function() {
        var cara = randInt(3, 7);
        var beth = cara * 2;
        var ageDiff = randInt(2, 5);
        var anna = beth + ageDiff;
        var opts = buildOpts(anna, [anna - 1, beth, anna + 1]);
        return { q: 'Anna is ' + ageDiff + ' years older than Beth. Beth is twice as old as Cara. Cara is ' + cara + '. How old is Anna?',
                 opts: opts, c: 0,
                 e: 'Cara=' + cara + '. Beth=2×' + cara + '=' + beth + '. Anna=' + beth + '+' + ageDiff + '=' + anna + '.' };
    }},
    { gen: function() {
        var total = randInt(20, 40);
        var setA = randInt(8, total - 5);
        var setB = randInt(6, total - 5);
        var both = randInt(2, Math.min(setA, setB) - 2);
        var either = setA + setB - both;
        var neither = total - either;
        var opts = buildOpts(neither, [neither + 2, neither - 1, neither + 4]);
        return { q: 'In a group of ' + total + ' students, ' + setA + ' like football and ' + setB + ' like tennis. ' + both + ' like both. How many like neither?',
                 opts: opts, c: 0,
                 e: 'Like at least one = ' + setA + '+' + setB + '−' + both + '=' + either + '. Neither = ' + total + '−' + either + '=' + neither + '.' };
    }},
    { gen: function() {
        var n = randInt(3, 9);
        var m = n + 1;
        var product = n * m;
        var opts = buildOpts(n + ' and ' + m, [(n-1) + ' and ' + (n+2), (n+1) + ' and ' + (n+2), (n-1) + ' and ' + n]);
        return { q: 'The product of two consecutive integers is ' + product + '. What are they?',
                 opts: opts, c: 0,
                 e: n + '×' + m + '=' + product + ' ✓.' };
    }},
    // Conceptual: Fibonacci pattern
    { question:'Find the pattern: 1, 1, 2, 3, 5, 8, … What is the next term?', options:['13','11','10','12'], answer:0, explanation:'Each term = sum of previous two (Fibonacci). 5+8=13.' },
    { gen: function() {
        var w = randInt(3, 9);
        var l = w + randInt(1, 6);
        var area = l * w;
        var perim = 2 * (l + w);
        var opts = buildOpts(l + 'cm × ' + w + 'cm', [(l+1) + 'cm × ' + (w-1) + 'cm', l + 'cm × ' + (w+1) + 'cm', (l+2) + 'cm × ' + (w-2) + 'cm']);
        return { q: 'A rectangle has area ' + area + 'cm² and perimeter ' + perim + 'cm. Find its dimensions.',
                 opts: opts, c: 0,
                 e: 'l+w=' + (l+w) + '. l×w=' + area + '. Try: ' + l + '+' + w + '=' + (l+w) + ' ✓ and ' + l + '×' + w + '=' + area + ' ✓.' };
    }},
    // Conceptual: counter-example (none exists)
    { question:'A counter-example to "all multiples of 4 are even" would be:', options:['There is none — it is always true','12','16','20'], answer:0, explanation:'All multiples of 4 ARE even (since 4=2×2), so no counter-example exists. The statement is true.' },
    { gen: function() {
        var p = randInt(2, 7), q = p + randInt(1, 5);
        var sum = p + q;
        var prod = p * q;
        var opts = buildOpts(p + ' and ' + q, [(p-1) + ' and ' + (q+1), (p+1) + ' and ' + (q-1), p + ' and ' + (q+1)]);
        return { q: 'If P+Q=' + sum + ' and P×Q=' + prod + ', what are P and Q?',
                 opts: opts, c: 0,
                 e: p + '+' + q + '=' + sum + ' ✓ and ' + p + '×' + q + '=' + prod + ' ✓.' };
    }},
    // Conceptual: identify flawed proof
    { question:'What is wrong with this proof? "n²+n is always odd. Proof: 2²+2=6, 3²+3=12 — both even."', options:['The examples given are even, not odd, so the claim is false','The proof needs more examples','The proof is correct','n must be odd'], answer:0, explanation:'The claim says "always odd" but the examples show even values — so the claim is immediately disproved.' },
    // Conceptual: algebraic proof of consecutive integers
    { question:'Show 5 consecutive integers always sum to a multiple of 5. Which algebra proves it?', options:['n+(n+1)+(n+2)+(n+3)+(n+4)=5n+10=5(n+2)','5n=5n','5+10+15+20+25=75','Cannot be proved algebraically'], answer:0, explanation:'Sum = 5n+10 = 5(n+2), which is always divisible by 5.' }
  ],

  // ── mi-16-4: Extra/Missing Info & Direction ──────────────────────────────
  'mi-16-4': [
    { question:'A farmer has 12 cows, 8 sheep and 5 pigs. He sells 3 cows. How many cows are left? Which information is extra?', options:['8 sheep and 5 pigs','12 cows','3 cows','None — all needed'], answer:0, explanation:'You only need 12 cows and 3 sold. The sheep and pigs are extra information. 12 − 3 = 9 cows.' },
    { question:'Ella walks 4 km north, then 7 km south. How far is she from the start and in which direction?', options:['3 km south','3 km north','11 km south','11 km north'], answer:0, explanation:'Net: 4 − 7 = −3 km (south). She is 3 km south of the start.' },
    { question:'"Tickets cost £8. A family buys some tickets. How much do they spend?" What is missing?', options:['The number of tickets','The price per ticket','The type of event','Nothing is missing'], answer:0, explanation:'We know the price per ticket but not how many were bought. Without that, we cannot calculate the total.' },
    { gen: function() {
        var east = randInt(3,12), west = randInt(1, east-1);
        var net = east - west;
        var opts = buildOpts(net + ' km west', [net + ' km east', (east+west) + ' km west', (east+west) + ' km east']);
        return { q: 'A drone flies ' + east + ' km east then ' + west + ' km west. To return to the start it must fly:',
                 opts: opts, c: 0,
                 e: 'Net displacement = ' + east + ' − ' + west + ' = ' + net + ' km east. To return, fly ' + net + ' km west.' };
    }},
    { gen: function() {
        var north = randInt(5,15), south = randInt(1, north-1);
        var net = north - south;
        var opts = buildOpts(net + ' km south', [net + ' km north', (north+south) + ' km south', north + ' km south']);
        return { q: 'A hiker walks ' + north + ' km north then ' + south + ' km south. How far and in what direction must she walk to get back?',
                 opts: opts, c: 0,
                 e: 'Net = ' + north + ' − ' + south + ' = ' + net + ' km north of start. Walk ' + net + ' km south to return.' };
    }},
    { gen: function() {
        var price = randInt(2,10), qty = randInt(3,8), extra = randInt(10,30);
        var total = price * qty;
        var opts = buildOpts('£' + extra + ' budget', ['£' + price + ' per item', qty + ' items', '£' + total + ' total']);
        return { q: 'Tom has a budget of £' + extra + '. He buys ' + qty + ' books at £' + price + ' each, spending £' + total + '. Which piece of information is extra?',
                 opts: opts, c: 0,
                 e: 'To find total cost we need price (£' + price + ') and quantity (' + qty + '). The budget of £' + extra + ' is extra information.' };
    }},
    { question:'"A rectangle has a perimeter of 28 cm. Find its area." Can this be solved?', options:['No — we need both length and width','Yes — area = 28 ÷ 2','Yes — area = 28²','Yes — area = 14'], answer:0, explanation:'Perimeter gives length + width = 14, but without knowing the individual dimensions, area cannot be determined.' },
    { gen: function() {
        var leg1 = randInt(2,8), leg2 = randInt(2,8);
        var net = leg1 - leg2;
        var dir = net > 0 ? 'east' : 'west';
        var retDir = net > 0 ? 'west' : 'east';
        var absNet = Math.abs(net);
        if(net === 0) { dir = 'at the start'; retDir = 'nowhere'; }
        var opts;
        if(net === 0) {
          opts = buildOpts('0 km — already at start', [leg1 + ' km east', leg1 + ' km west', (leg1+leg2) + ' km east']);
        } else {
          opts = buildOpts(absNet + ' km ' + retDir, [absNet + ' km ' + dir, (leg1+leg2) + ' km ' + retDir, leg1 + ' km ' + retDir]);
        }
        return { q: 'A boat sails ' + leg1 + ' km east then ' + leg2 + ' km west. What journey returns it to the start?',
                 opts: opts, c: 0,
                 e: 'Net displacement = ' + leg1 + ' − ' + leg2 + ' = ' + net + ' km. ' + (net===0 ? 'Already at start.' : 'Return: ' + absNet + ' km ' + retDir + '.') };
    }},
    { question:'"Sam scored 85% on his test. His friend scored 72%. How many marks did Sam get?" What is missing?', options:['The total marks available','The difference in scores','The pass mark','Nothing is missing'], answer:0, explanation:'85% of what? We need the total marks to calculate Sam\'s actual score. 85% of 40 = 34, but 85% of 100 = 85.' },
    { question:'A robot moves: 6 m north, 2 m south, 5 m north, 4 m south. How far north of the start is it?', options:['5 m','6 m','17 m','11 m'], answer:0, explanation:'North: 6+5=11 m. South: 2+4=6 m. Net = 11−6 = 5 m north.' }
  ],

  // â"€â"€ mi-17-1: Metric Units â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  'mi-17-1': [
    { gen: function() {
        var v = pickFrom([1.5, 2.5, 3.5, 4.5, 0.5, 2, 3, 4, 6]);
        var ans = v * 100;
        var opts = buildOpts(ans + 'cm', [(v * 10) + 'cm', (v * 1000) + 'cm', v + 'cm']);
        return { q: 'How many centimetres in ' + v + ' metres?',
                 opts: opts, c: 0,
                 e: '1 m = 100 cm. ' + v + ' × 100 = ' + ans + ' cm.' };
    }},
    { gen: function() {
        var g = pickFrom([500, 1500, 2500, 3500, 4500, 750, 1250, 2000, 3000]);
        var ans = g / 1000;
        var opts = buildOpts(ans + ' kg', [(g / 100) + ' kg', (g * 1000) + ' kg', (g / 10) + ' kg']);
        return { q: 'Convert ' + g.toLocaleString() + ' grams to kilograms.',
                 opts: opts, c: 0,
                 e: '1 kg = 1000 g. ' + g + ' ÷ 1000 = ' + ans + ' kg.' };
    }},
    { gen: function() {
        var l = pickFrom([1.5, 2.4, 0.75, 3.2, 0.5, 2, 3]);
        var ans = l * 1000;
        var opts = buildOpts(ans + ' ml', [(l * 100) + ' ml', (l * 10000) + ' ml', (l / 1000) + ' ml']);
        return { q: 'Convert ' + l + ' litres to millilitres.',
                 opts: opts, c: 0,
                 e: '1 litre = 1000 ml. ' + l + ' × 1000 = ' + ans + ' ml.' };
    }},
    { gen: function() {
        var km = pickFrom([1.5, 2.3, 0.8, 3.1, 4.2]);
        var m1 = km * 1000;
        var m2 = pickFrom([m1 - 50, m1 - 200, m1 + 100, m1 - 500]).filter(function(x){ return x > 0; })[0] || (m1 - 100);
        var ansStr = km + ' km';
        var m2str = m2 + ' m';
        var opts = buildOpts(km + ' km (' + m1 + 'm vs ' + m2 + 'm)', [m2str, 'They are equal', 'Cannot compare']);
        // simpler version
        var opts2 = buildOpts(km + ' km', [m2 + ' m', 'They are equal', 'Cannot compare']);
        return { q: 'Which is larger: ' + km + ' km or ' + m2 + ' m?',
                 opts: opts2, c: 0,
                 e: km + ' km = ' + m1 + ' m ' + (m1 > m2 ? '>' : '<') + ' ' + m2 + ' m.' };
    }},
    { gen: function() {
        var mm = pickFrom([85, 35, 120, 75, 45, 55, 165, 250]);
        var ans = mm / 10;
        var opts = buildOpts(ans + ' cm', [(mm * 10) + ' cm', (mm / 100) + ' cm', (mm / 1000) + ' cm']);
        return { q: 'Convert ' + mm + 'mm to cm.',
                 opts: opts, c: 0,
                 e: '1 cm = 10 mm. ' + mm + ' ÷ 10 = ' + ans + ' cm.' };
    }},
    { gen: function() {
        var litres = pickFrom([500000, 750000, 1000000, 250000, 1500000]);
        var ans = litres / 1000;
        var opts = buildOpts(ans + ' m³', [(litres / 100) + ' m³', (litres * 1000) + ' m³', (litres / 10000) + ' m³']);
        return { q: 'A swimming pool holds ' + litres.toLocaleString() + ' litres. How many m³ is this?',
                 opts: opts, c: 0,
                 e: '1 m³ = 1,000 litres. ' + litres.toLocaleString() + ' ÷ 1,000 = ' + ans + ' m³.' };
    }},
    { gen: function() {
        var kg = pickFrom([1.5, 2.3, 3.7, 0.8, 4.2, 2.8]);
        var ans = kg * 1000;
        var opts = buildOpts(ans + ' g', [(kg * 100) + ' g', (kg / 1000) + ' g', (kg * 10000) + ' g']);
        return { q: 'A parcel weighs ' + kg + ' kg. How many grams is this?',
                 opts: opts, c: 0,
                 e: kg + ' × 1000 = ' + ans + ' g.' };
    }},
    { gen: function() {
        var km = pickFrom([3, 4.5, 6, 9, 12]);
        var frac = pickFrom([2, 3, 4]);
        var m = km * 1000;
        var ans = m / frac;
        var opts = buildOpts(ans + ' m', [(ans + 200) + ' m', (ans - 100) + ' m', km + ' m']);
        return { q: 'A road is ' + km + ' km long. A 1/' + frac + ' section is repaired. How many metres is repaired?',
                 opts: opts, c: 0,
                 e: km + ' km = ' + m + ' m. ' + m + ' ÷ ' + frac + ' = ' + ans + ' m.' };
    }},
    { gen: function() {
        var cupMl = pickFrom([200, 250, 300, 500]);
        var jugL = pickFrom([1, 1.5, 2, 2.5, 3]);
        var jugMl = jugL * 1000;
        var ans = jugMl / cupMl;
        var opts = buildOpts(ans, [ans - 1, ans + 1, ans * 2]);
        return { q: 'A jug holds ' + jugL + ' litre' + (jugL > 1 ? 's' : '') + '. How many ' + cupMl + 'ml cups can be filled?',
                 opts: opts, c: 0,
                 e: jugMl + ' ÷ ' + cupMl + ' = ' + ans + ' cups.' };
    }},
    { gen: function() {
        var raceKm = pickFrom([5, 10, 15, 20, 42]);
        var completedM = randInt(Math.floor(raceKm * 0.3 * 10), Math.floor(raceKm * 0.85 * 10)) * 100;
        var totalM = raceKm * 1000;
        var remainM = totalM - completedM;
        var remainKm = remainM / 1000;
        var opts = buildOpts(remainKm + ' km', [(remainKm + 0.5) + ' km', (remainKm - 0.5) + ' km', (remainKm + 1) + ' km']);
        return { q: 'A race is ' + raceKm + ' km. A runner has completed ' + completedM.toLocaleString() + ' m. How many km remain?',
                 opts: opts, c: 0,
                 e: raceKm + ' km = ' + totalM.toLocaleString() + ' m. ' + totalM.toLocaleString() + ' − ' + completedM.toLocaleString() + ' = ' + remainM.toLocaleString() + ' m = ' + remainKm + ' km.' };
    }}
  ],

  // â"€â"€ mi-17-2: Imperial Units â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  'mi-17-2': [
    { gen: function() {
        var feet = randInt(2, 8);
        var ans = feet * 12;
        var opts = buildOpts(ans, [feet * 10, feet * 12 - 12, feet * 12 + 12]);
        return { q: 'How many inches are in ' + feet + ' feet?',
                 opts: opts, c: 0,
                 e: '1 foot = 12 inches. ' + feet + ' × 12 = ' + ans + ' inches.' };
    }},
    { gen: function() {
        var yards = randInt(2, 8);
        var ans = yards * 3;
        var opts = buildOpts(ans + ' feet', [(yards * 2) + ' feet', (yards * 4) + ' feet', (yards + 3) + ' feet']);
        return { q: 'Convert ' + yards + ' yards to feet.',
                 opts: opts, c: 0,
                 e: '1 yard = 3 feet. ' + yards + ' × 3 = ' + ans + ' feet.' };
    }},
    { gen: function() {
        var lbs = pickFrom([2, 2.5, 3, 3.5, 4, 1.5]);
        var ans = lbs * 16;
        var opts = buildOpts(ans + ' oz', [(lbs * 14) + ' oz', (lbs * 12) + ' oz', (lbs * 16 + 8) + ' oz']);
        return { q: 'How many ounces are in ' + lbs + ' pounds?',
                 opts: opts, c: 0,
                 e: '1 pound = 16 oz. ' + lbs + ' × 16 = ' + ans + ' oz.' };
    }},
    { gen: function() {
        var stone = randInt(2, 6);
        var ans = stone * 14;
        var opts = buildOpts(ans + ' lb', [(stone * 12) + ' lb', (stone * 16) + ' lb', (stone * 14 + 7) + ' lb']);
        return { q: 'Convert ' + stone + ' stone to pounds.',
                 opts: opts, c: 0,
                 e: '1 stone = 14 lb. ' + stone + ' × 14 = ' + ans + ' lb.' };
    }},
    { gen: function() {
        var gallons = randInt(2, 6);
        var ans = gallons * 8;
        var opts = buildOpts(ans + ' pints', [(gallons * 4) + ' pints', (gallons * 6) + ' pints', (gallons * 8 + 8) + ' pints']);
        return { q: 'How many pints are in ' + gallons + ' gallons?',
                 opts: opts, c: 0,
                 e: '1 gallon = 8 pints. ' + gallons + ' × 8 = ' + ans + ' pints.' };
    }},
    { gen: function() {
        var ft = randInt(3, 9), inch = randInt(1, 11);
        var ans = ft * 12 + inch;
        var opts = buildOpts(ans + ' inches', [(ft * 12) + ' inches', (ft * 12 + inch + 6) + ' inches', (ft * 12 - inch) + ' inches']);
        return { q: 'A piece of wood is ' + ft + ' feet ' + inch + ' inches long. Express this in inches.',
                 opts: opts, c: 0,
                 e: ft + ' × 12 + ' + inch + ' = ' + (ft*12) + ' + ' + inch + ' = ' + ans + ' inches.' };
    }},
    { gen: function() {
        var oz = randInt(2, 8) * 16 + pickFrom([0, 4, 8, 12]);
        var ans = oz / 16;
        var opts = buildOpts(ans + ' lb', [(ans + 0.5) + ' lb', (ans - 0.5) + ' lb', (oz / 14).toFixed(1) + ' lb']);
        return { q: 'A bag of flour weighs ' + oz + ' ounces. How many pounds is this?',
                 opts: opts, c: 0,
                 e: oz + ' ÷ 16 = ' + ans + ' pounds.' };
    }},
    { gen: function() {
        var pints = randInt(1, 5);
        var ans = pints * 20;
        var opts = buildOpts(ans + ' fl oz', [(pints * 16) + ' fl oz', (pints * 8) + ' fl oz', (pints * 20 + 10) + ' fl oz']);
        return { q: 'How many fluid ounces are in ' + pints + ' pint' + (pints > 1 ? 's' : '') + '? (1 pint = 20 fl oz)',
                 opts: opts, c: 0,
                 e: pints + ' × 20 = ' + ans + ' fluid ounces.' };
    }},
    { gen: function() {
        var yards = randInt(2, 6), extraFeet = randInt(1, 2);
        var ans = yards * 3 + extraFeet;
        var opts = buildOpts(ans + ' feet', [(yards * 3) + ' feet', (yards * 3 + extraFeet + 3) + ' feet', (yards * 4 + extraFeet) + ' feet']);
        return { q: 'A fence is ' + yards + ' yards ' + extraFeet + ' foot' + (extraFeet > 1 ? '' : '') + ' long. How long in feet?',
                 opts: opts, c: 0,
                 e: yards + ' yards = ' + (yards*3) + ' feet. ' + (yards*3) + ' + ' + extraFeet + ' = ' + ans + ' feet.' };
    }},
    { gen: function() {
        var stone = randInt(1, 3), lbExtra = randInt(1, 10);
        var totalLb = stone * 14 + lbExtra;
        var compare = totalLb + randInt(1, 5);
        var heavier = compare + ' pounds';
        var opts = buildOpts(heavier, [stone + ' stone ' + lbExtra + ' pounds', 'They are equal', 'Cannot compare']);
        return { q: 'Which is heavier: ' + stone + ' stone ' + lbExtra + ' pounds or ' + compare + ' pounds?',
                 opts: opts, c: 0,
                 e: stone + ' stone ' + lbExtra + ' lb = ' + (stone*14) + '+' + lbExtra + '=' + totalLb + ' lb. ' + compare + ' lb > ' + totalLb + ' lb.' };
    }}
  ],

  // â"€â"€ mi-17-3: Convert Metric & Imperial â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€â"€
  'mi-17-3': [
    { gen: function() {
        var miles = pickFrom([5, 8, 10, 15, 20, 25, 30]);
        var ans = Math.round(miles * 1.6 * 10) / 10;
        var opts = buildOpts(ans + ' km', [miles + ' km', Math.round(miles * 1.6 * 2 * 10)/10 + ' km', Math.round(miles * 0.6 * 10)/10 + ' km']);
        return { q: 'Approximately how many km is ' + miles + ' miles? (1 mile ≈ 1.6 km)',
                 opts: opts, c: 0,
                 e: miles + ' × 1.6 = ' + ans + ' km.' };
    }},
    { gen: function() {
        var kg = pickFrom([3, 4, 5, 6, 7, 8, 10]);
        var lbs = Math.round(kg * 2.2 * 10) / 10;
        var opts = buildOpts(lbs + ' lb', [Math.round(lbs * 2 * 10)/10 + ' lb', Math.round(kg * 2.2 / 2 * 10)/10 + ' lb', (lbs + 2) + ' lb']);
        return { q: 'Approximately how many pounds is ' + kg + ' kg? (1 kg ≈ 2.2 lb)',
                 opts: opts, c: 0,
                 e: kg + ' × 2.2 = ' + lbs + ' lb.' };
    }},
    { gen: function() {
        var litres = pickFrom([2, 3, 4, 5, 6]);
        var pints = Math.round(litres * 1.75 * 10) / 10;
        var opts = buildOpts(pints + ' pints', [litres + ' pints', Math.round(litres * 1.75 * 2 * 10)/10 + ' pints', Math.round(pints / 2 * 10)/10 + ' pints']);
        return { q: 'Convert ' + litres + ' litres to pints. (1 litre ≈ 1.75 pints)',
                 opts: opts, c: 0,
                 e: litres + ' × 1.75 = ' + pints + ' pints.' };
    }},
    { gen: function() {
        var ft = randInt(4, 6), inch = randInt(0, 11);
        var totalInch = ft * 12 + inch;
        var cm = Math.round(totalInch * 2.54);
        var opts = buildOpts(cm + ' cm', [(ft * 30) + ' cm', (totalInch * 2) + ' cm', (cm + 10) + ' cm']);
        return { q: 'A person is ' + ft + ' feet ' + inch + ' inches tall. (1 inch ≈ 2.54 cm). Approximately how tall in cm?',
                 opts: opts, c: 0,
                 e: totalInch + ' inches × 2.54 = ' + cm + ' cm.' };
    }},
    { gen: function() {
        var miles = pickFrom([10, 15, 20, 25, 30, 50]);
        var ans = Math.round(miles * 1.6);
        var opts = buildOpts(ans + ' km', [miles + ' km', Math.round(miles * 0.6) + ' km', (ans + 10) + ' km']);
        return { q: 'Convert ' + miles + ' miles to km (1 mile ≈ 1.6 km).',
                 opts: opts, c: 0,
                 e: miles + ' × 1.6 = ' + ans + ' km.' };
    }},
    { gen: function() {
        var g = pickFrom([250, 500, 750, 1000, 1500]);
        var kg = g / 1000;
        var lbs = Math.round(kg * 2.2 * 10) / 10;
        var opts = buildOpts(lbs + ' lb', [(g * 2.2).toFixed(1) + ' lb', Math.round(kg * 2.2 * 2 * 10)/10 + ' lb', (lbs + 1) + ' lb']);
        return { q: 'A recipe needs ' + g + 'g of flour. Approximately how many pounds is this? (1 kg ≈ 2.2 lb)',
                 opts: opts, c: 0,
                 e: g + 'g = ' + kg + ' kg. ' + kg + ' × 2.2 = ' + lbs + ' lb.' };
    }},
    { gen: function() {
        var mph = pickFrom([40, 50, 60, 70, 80, 90, 100]);
        var kmh = Math.round(mph * 1.6);
        var opts = buildOpts(kmh + ' km/h', [mph + ' km/h', Math.round(mph * 0.6) + ' km/h', (kmh + 10) + ' km/h']);
        return { q: 'A car travels ' + mph + ' mph. Convert to km/h (1 mile ≈ 1.6 km).',
                 opts: opts, c: 0,
                 e: mph + ' × 1.6 = ' + kmh + ' km/h.' };
    }},
    { gen: function() {
        var gallons = randInt(1, 4);
        var litresPerGallon = 4.5;
        var galLitres = Math.round(gallons * litresPerGallon * 10) / 10;
        var compareLitres = pickFrom([galLitres - 1, galLitres + 2, galLitres - 3, galLitres + 4]).filter(function(x){ return x > 0; })[0];
        var galWins = galLitres > compareLitres;
        var ansStr = galWins ? gallons + ' gallon' + (gallons > 1 ? 's' : '') : compareLitres + ' litres';
        var opts = buildOpts(ansStr, [galWins ? compareLitres + ' litres' : gallons + ' gallons', 'They are equal', 'Cannot compare']);
        return { q: 'Which is more: ' + gallons + ' gallon' + (gallons > 1 ? 's' : '') + ' or ' + compareLitres + ' litres? (1 gallon ≈ 4.5 litres)',
                 opts: opts, c: 0,
                 e: gallons + ' gallons ≈ ' + galLitres + ' litres ' + (galWins ? '>' : '<') + ' ' + compareLitres + ' litres. So ' + ansStr + ' is more.' };
    }},
    { gen: function() {
        var kg = pickFrom([2.5, 3, 3.2, 4, 5, 3.5]);
        var lbs = Math.round(kg * 2.2 * 10) / 10;
        var opts = buildOpts(lbs + ' lb', [Math.round(kg * 2) + ' lb', Math.round(lbs * 2) + ' lb', (lbs - 1) + ' lb']);
        return { q: 'A baby weighs ' + kg + ' kg. About how many pounds is this?',
                 opts: opts, c: 0,
                 e: kg + ' × 2.2 ≈ ' + lbs + ' lb.' };
    }},
    { gen: function() {
        var km = pickFrom([10, 16, 20, 25, 32, 40, 50]);
        var miles = Math.round(km / 1.6 * 10) / 10;
        var opts = buildOpts('About ' + miles + ' miles', [km + ' miles', Math.round(km * 1.6) + ' miles', Math.round(miles / 2 * 10)/10 + ' miles']);
        return { q: 'A road sign says ' + km + ' km to the next town. About how many miles is this?',
                 opts: opts, c: 0,
                 e: km + ' ÷ 1.6 ≈ ' + miles + ' miles.' };
    }}
  ],

  // ── mi-17-4: Area/Volume Units & Temperature ─────────────────────────────
  'mi-17-4': [
    { gen: function() {
        var m2 = randInt(2, 10);
        var ans = m2 * 10000;
        var opts = buildOpts(ans + ' cm²', [(m2 * 100) + ' cm²', (m2 * 1000) + ' cm²', m2 + ' cm²']);
        return { q: 'Convert ' + m2 + ' m² to cm².', opts: opts, c: 0,
                 e: '1 m² = 10,000 cm². ' + m2 + ' × 10,000 = ' + ans + ' cm².' };
    }},
    { gen: function() {
        var cm3 = pickFrom([500, 750, 1000, 1500, 2000, 2500, 3000]);
        var litres = cm3 / 1000;
        var opts = buildOpts(litres + ' litres', [(cm3/100) + ' litres', (cm3/10) + ' litres', cm3 + ' litres']);
        return { q: cm3 + ' cm³ = how many litres?', opts: opts, c: 0,
                 e: '1,000 cm³ = 1 litre. ' + cm3 + ' ÷ 1,000 = ' + litres + ' litres.' };
    }},
    { gen: function() {
        var a = -randInt(1, 15), b = -randInt(1, 15);
        while (a === b) b = -randInt(1, 15);
        var warmer = Math.max(a, b), colder = Math.min(a, b);
        var opts = buildOpts(warmer + '°C', [colder + '°C', '0°C', (a + b) + '°C']);
        return { q: 'Which is warmer: ' + a + '°C or ' + b + '°C?', opts: opts, c: 0,
                 e: warmer + ' > ' + colder + ' (closer to 0), so ' + warmer + '°C is warmer.' };
    }},
    { gen: function() {
        var cm2 = pickFrom([20000, 30000, 50000, 80000, 100000]);
        var ans = cm2 / 10000;
        var opts = buildOpts(ans + ' m²', [(cm2/100) + ' m²', (cm2/1000) + ' m²', cm2 + ' m²']);
        return { q: 'Convert ' + cm2.toLocaleString() + ' cm² to m².', opts: opts, c: 0,
                 e: '÷ 10,000. ' + cm2.toLocaleString() + ' ÷ 10,000 = ' + ans + ' m².' };
    }},
    { gen: function() {
        var litres = randInt(1, 8);
        var ans = litres * 1000;
        var opts = buildOpts(ans + ' cm³', [(litres * 100) + ' cm³', (litres * 10) + ' cm³', litres + ' cm³']);
        return { q: litres + ' litres = how many cm³?', opts: opts, c: 0,
                 e: '1 litre = 1,000 cm³. ' + litres + ' × 1,000 = ' + ans + ' cm³.' };
    }},
    { question:'A measurement is recorded as 4.7 cm. To the nearest mm, this could be from:', options:['4.65 cm to 4.75 cm','4.6 cm to 4.8 cm','4.0 cm to 5.0 cm','4.70 cm to 4.79 cm'], answer:0, explanation:'Rounded to 1 d.p., the true value is between 4.65 and 4.75 cm.' },
    { question:'1 m³ = how many litres?', options:['1,000 litres','100 litres','10 litres','10,000 litres'], answer:0, explanation:'1 m³ = 1,000,000 cm³. 1,000,000 ÷ 1,000 = 1,000 litres.' },
    { question:'The temperature rises from −5°C to 3°C. What is the rise?', options:['8°C','2°C','−2°C','3°C'], answer:0, explanation:'From −5 to 0 is 5°C, then 0 to 3 is 3°C. Total rise = 5 + 3 = 8°C.' },
    { question:'Which unit is most precise for measuring the length of a pencil?', options:['mm','cm','m','km'], answer:0, explanation:'mm gives the most precise measurement for a small object like a pencil.' },
    { question:'A fish tank is 50 cm × 30 cm × 20 cm. What is its capacity in litres?', options:['30 litres','300 litres','3 litres','3000 litres'], answer:0, explanation:'Volume = 50 × 30 × 20 = 30,000 cm³. 30,000 ÷ 1,000 = 30 litres.' }
  ]

});
