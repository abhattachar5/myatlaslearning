# Year 10 — not yet built

Content for Year 10 goes here. Follow the canonical layout in `content/_TEMPLATE/`
and the "add a new year" steps in `docs/ARCHITECTURE.md`.

When you add it: create the subject folders here, add a `y10` block to
`core/manifest.js`, and add `'y10'` to `CONFIG.active` in `core/config.js`.

(This README exists so the folder is tracked by Git — empty folders are not.)
