// content/<year>/<subject>/generators/01.js — CANONICAL TEMPLATE (copy, don't load this).
// OPTIONAL. Test generators — loaded ONLY on test.html / exam.html (window.ATLAS_NEEDS_TESTS).
// They populate TEST_GENERATORS (declared by engine/test-generator.js, which loads first).

// (function () {
//   if (typeof TEST_GENERATORS === 'undefined') return;
//   // Bespoke generator:
//   TEST_GENERATORS['m9i-01-1'] = function (opts) { /* return [ {q,opts,c,e}, … ] */ };
//   // …or reuse another year's generator by reference:
//   // TEST_GENERATORS['m9i-01-1'] = TEST_GENERATORS['mi-01-1'];
// })();
