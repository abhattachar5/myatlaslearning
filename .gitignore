# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.log

# Node (only used for local verification scripts, never deployed)
node_modules/

# Local-only dev harness scratch files
_dev-*.html
_scratch/
serve.json
