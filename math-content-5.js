// math-content-5.js — Flashcards & Questions for Topics 18-21
// Topic 18: Money & Consumer Maths | Topic 19: Time
// Topic 20: Coordinate Plane | Topic 21: Number Sequences

// ─── TOPIC 18: MONEY & CONSUMER MATHS ────────────────────────────────────────

FLASHCARDS.push(
  // mi-18-1: Add & Subtract Money
  { islandId:'mi-18-1', front:'How do you find the change from a purchase?', back:'Change = Amount given − Total cost. E.g. Pay £5 for £3.72 → Change = £5.00 − £3.72 = £1.28.' },
  { islandId:'mi-18-1', front:'Emma buys two items costing £2.45 and £3.80. She pays with a £10 note. How much change?', back:'Total = £2.45 + £3.80 = £6.25. Change = £10.00 − £6.25 = £3.75.' },
  { islandId:'mi-18-1', front:'How do you add money with different numbers of pence digits?', back:'Align decimal points and add zeros as placeholders. E.g. £4.5 → £4.50. Then add column by column.' },

  // mi-18-2: Sale Prices & Unit Prices
  { islandId:'mi-18-2', front:'How do you calculate a sale price after a 25% discount?', back:'Sale price = Original price × (1 − 0.25) = Original × 0.75. Or find 25% and subtract.' },
  { islandId:'mi-18-2', front:'What is the unit price and why is it useful?', back:'Unit price = Total cost ÷ Number of units. It lets you compare value across different pack sizes.' },
  { islandId:'mi-18-2', front:'A 400 g jar costs £2.80 and a 600 g jar costs £3.90. Which is better value?', back:'400g: £2.80÷400=0.7p/g. 600g: £3.90÷600=0.65p/g. The 600g jar is better value.' },

  // mi-18-3: VAT & Tips
  { islandId:'mi-18-3', front:'What is VAT and what is the standard UK rate?', back:'VAT (Value Added Tax) is a tax added to goods and services. The standard rate in the UK is 20%.' },
  { islandId:'mi-18-3', front:'A meal costs £40 before VAT. What is the total with 20% VAT?', back:'VAT = 20% of £40 = £8. Total = £40 + £8 = £48. Or £40 × 1.2 = £48.' },
  { islandId:'mi-18-3', front:'A restaurant bill is £55. You leave a 15% tip. How much is the tip?', back:'Tip = 15% of £55 = 0.15 × £55 = £8.25.' },

  // mi-18-4: Multiply & Divide Money & Coins
  { islandId:'mi-18-4', front:'What is £3.45 × 6?', back:'£3.45 × 6:\n345p × 6 = 2,070p = £20.70\n\nOr: £3 × 6 = £18, 45p × 6 = £2.70.\n£18 + £2.70 = £20.70' },
  { islandId:'mi-18-4', front:'Share £15.60 equally among 4 people.', back:'£15.60 ÷ 4 = £3.90 each.\n\n1,560p ÷ 4 = 390p = £3.90.\n\nCheck: £3.90 × 4 = £15.60 ✓' },
  { islandId:'mi-18-4', front:'You have 50p, 20p and 10p coins. How can you make exactly 80p?', back:'Several ways:\n• 1×50p + 1×20p + 1×10p = 80p\n• 4×20p = 80p\n• 1×50p + 3×10p = 80p\n• 2×20p + 4×10p = 80p\n\nAlways check the total matches.' }
);

// ─── TOPIC 19: TIME ───────────────────────────────────────────────────────────

FLASHCARDS.push(
  // mi-19-1: Elapsed Time
  { islandId:'mi-19-1', front:'How do you calculate elapsed time between 10:45 and 13:20?', back:'From 10:45 to 13:00 = 2h 15min. From 13:00 to 13:20 = 20min. Total = 2h 35min.' },
  { islandId:'mi-19-1', front:'A film starts at 14:50 and lasts 2h 35min. When does it end?', back:'14:50 + 2h = 16:50. 16:50 + 35min = 17:25.' },
  { islandId:'mi-19-1', front:'How many minutes are in 3 hours 20 minutes?', back:'3 × 60 + 20 = 180 + 20 = 200 minutes.' },

  // mi-19-2: 12-Hour & 24-Hour Time
  { islandId:'mi-19-2', front:'How do you convert 3:45 pm to 24-hour time?', back:'pm times after 12:00: add 12. 3:45 pm = 15:45.' },
  { islandId:'mi-19-2', front:'How do you convert 07:20 (24-hour) to 12-hour time?', back:'07:20 is before 12:00, so it is 7:20 am.' },
  { islandId:'mi-19-2', front:'What is midnight in 24-hour clock?', back:'Midnight = 00:00. Midday (noon) = 12:00.' },

  // mi-19-3: Time Zones & Schedules
  { islandId:'mi-19-3', front:'London is GMT+0. New York is GMT−5. What is the time in New York when it is 14:00 in London?', back:'14:00 − 5 hours = 09:00 in New York.' },
  { islandId:'mi-19-3', front:'A train timetable shows departure 08:47 and arrival 11:23. How long is the journey?', back:'From 08:47 to 11:23: 2 hours 36 minutes.' },
  { islandId:'mi-19-3', front:'What does UTC stand for and why is it used?', back:'Coordinated Universal Time — the global time standard used to define time zones around the world.' }
);

// ─── TOPIC 20: COORDINATE PLANE ──────────────────────────────────────────────

FLASHCARDS.push(
  // mi-20-1: Plot Points on a Coordinate Plane
  { islandId:'mi-20-1', front:'What does the coordinate pair (3, −2) mean?', back:'Move 3 units right along the x-axis, then 2 units down on the y-axis. x-coordinate always comes first.' },
  { islandId:'mi-20-1', front:'Where is the origin on a coordinate plane?', back:'The origin is the point (0, 0) where the x-axis and y-axis intersect.' },
  { islandId:'mi-20-1', front:'What is the difference between the x-axis and the y-axis?', back:'The x-axis is horizontal (left/right). The y-axis is vertical (up/down). They meet at the origin.' },

  // mi-20-2: Quadrants & Axes
  { islandId:'mi-20-2', front:'What are the signs of coordinates in Quadrant III?', back:'Both x and y are negative in Quadrant III (bottom-left). E.g. (−3, −4).' },
  { islandId:'mi-20-2', front:'In which quadrant is the point (−5, 3)?', back:'x is negative, y is positive → Quadrant II (top-left).' },
  { islandId:'mi-20-2', front:'What is special about points on the x-axis?', back:'Points on the x-axis have a y-coordinate of 0. E.g. (4, 0) is on the x-axis.' },

  // mi-20-3: Coordinate Maps & Directions
  { islandId:'mi-20-3', front:'How do you find the distance between (2, 3) and (2, 7)?', back:'Same x-coordinate, so count vertically: |7 − 3| = 4 units.' },
  { islandId:'mi-20-3', front:'Point A is at (1, 2). You move right 4 and down 3. Where do you land?', back:'New point: (1+4, 2−3) = (5, −1).' },
  { islandId:'mi-20-3', front:'How do you find the horizontal distance between (−3, 0) and (5, 0)?', back:'|5 − (−3)| = |5 + 3| = 8 units.' }
);

// ─── TOPIC 21: NUMBER SEQUENCES ───────────────────────────────────────────────

FLASHCARDS.push(
  // mi-21-1: Arithmetic Sequences
  { islandId:'mi-21-1', front:'What is an arithmetic sequence?', back:'A sequence where the difference between consecutive terms is constant. This constant is called the common difference (d).' },
  { islandId:'mi-21-1', front:'Find the nth-term formula for: 5, 8, 11, 14, …', back:'d = 3. First term = 5. nth term = 3n + 2. Check: n=1 → 3(1)+2=5 ✓' },
  { islandId:'mi-21-1', front:'What is the common difference in: 20, 15, 10, 5, …?', back:'d = 15 − 20 = −5. The sequence is decreasing by 5 each time.' },

  // mi-21-2: Geometric Sequences
  { islandId:'mi-21-2', front:'What is a geometric sequence?', back:'A sequence where each term is multiplied by a fixed number called the common ratio (r). E.g. 2, 6, 18, 54 has r = 3.' },
  { islandId:'mi-21-2', front:'Find the next two terms: 3, 6, 12, 24, …', back:'r = 2. Next: 24×2=48, then 48×2=96. Sequence: 3, 6, 12, 24, 48, 96.' },
  { islandId:'mi-21-2', front:'How do you tell if a sequence is arithmetic or geometric?', back:'Check differences (subtract): constant → arithmetic. Check ratios (divide): constant → geometric.' },

  // mi-21-3: Expressions for Sequences
  { islandId:'mi-21-3', front:'The nth-term rule is 4n − 1. Find the 6th term.', back:'4(6) − 1 = 24 − 1 = 23.' },
  { islandId:'mi-21-3', front:'Write the nth-term expression for: 3, 5, 7, 9, …', back:'d=2, so start with 2n. Term 1: 2(1)=2, but we need 3. So nth term = 2n + 1.' },
  { islandId:'mi-21-3', front:'Is 100 a term in the sequence with nth term 3n + 1?', back:'Solve 3n + 1 = 100 → 3n = 99 → n = 33. Yes, it is the 33rd term.' }
);

// ─── QUESTIONS ────────────────────────────────────────────────────────────────

Object.assign(QUESTIONS, {

  // ── mi-18-1: Add & Subtract Money ─────────────────────────────────────────
  'mi-18-1': [
    { gen: function() {
        var a = randInt(1, 8) + 0.99, b = randInt(1, 5) + pickFrom([0.25, 0.50, 0.75, 0.99]);
        a = parseFloat(a.toFixed(2)); b = parseFloat(b.toFixed(2));
        var ans = parseFloat((a + b).toFixed(2));
        var opts = buildOpts('£' + ans.toFixed(2), ['£' + (ans - 0.10).toFixed(2), '£' + (ans + 0.10).toFixed(2), '£' + (ans + 1.00).toFixed(2)]);
        return { q: 'A book costs £' + a.toFixed(2) + ' and a pen costs £' + b.toFixed(2) + '. What is the total cost?',
                 opts: opts, c: 0,
                 e: '£' + a.toFixed(2) + ' + £' + b.toFixed(2) + ' = £' + ans.toFixed(2) + '.' };
    }},
    { gen: function() {
        var paid = pickFrom([5, 10, 20]);
        var cost = parseFloat((paid - randInt(1, paid - 1) - pickFrom([0.01, 0.10, 0.27, 0.43, 0.55, 0.73])).toFixed(2));
        var change = parseFloat((paid - cost).toFixed(2));
        var opts = buildOpts('£' + change.toFixed(2), ['£' + (change + 0.10).toFixed(2), '£' + (change - 0.10).toFixed(2), '£' + (change + 1.00).toFixed(2)]);
        return { q: 'You pay £' + paid.toFixed(2) + ' for items costing £' + cost.toFixed(2) + '. What change do you receive?',
                 opts: opts, c: 0,
                 e: '£' + paid.toFixed(2) + ' − £' + cost.toFixed(2) + ' = £' + change.toFixed(2) + '.' };
    }},
    { gen: function() {
        var a = randInt(2, 5) + pickFrom([0.25, 0.50, 0.75]);
        var b = randInt(1, 3) + pickFrom([0.10, 0.20, 0.30, 0.40]);
        a = parseFloat(a.toFixed(2)); b = parseFloat(b.toFixed(2));
        var budget = parseFloat((a + b + randInt(1, 3) + pickFrom([0.10, 0.20, 0.50])).toFixed(2));
        var spent = parseFloat((a + b).toFixed(2));
        var left = parseFloat((budget - spent).toFixed(2));
        var opts = buildOpts('£' + left.toFixed(2), ['£' + (left + 1.00).toFixed(2), '£' + (left - 0.20).toFixed(2), '£' + (left + 0.50).toFixed(2)]);
        return { q: 'Mia buys lunch for £' + a.toFixed(2) + ' and a drink for £' + b.toFixed(2) + '. She has £' + budget.toFixed(2) + '. How much does she have left?',
                 opts: opts, c: 0,
                 e: '£' + a.toFixed(2) + ' + £' + b.toFixed(2) + ' = £' + spent.toFixed(2) + '. £' + budget.toFixed(2) + ' − £' + spent.toFixed(2) + ' = £' + left.toFixed(2) + '.' };
    }},
    { gen: function() {
        var paid = pickFrom([10, 20, 50]);
        var cost = parseFloat((paid - randInt(2, paid - 2) - pickFrom([0.01, 0.15, 0.27, 0.45, 0.55])).toFixed(2));
        var change = parseFloat((paid - cost).toFixed(2));
        var opts = buildOpts('£' + change.toFixed(2), ['£' + (change + 1.00).toFixed(2), '£' + (change - 0.45).toFixed(2), '£' + (change + 0.10).toFixed(2)]);
        return { q: 'Tom pays £' + paid.toFixed(2) + ' for items totalling £' + cost.toFixed(2) + '. How much change does he get?',
                 opts: opts, c: 0,
                 e: '£' + paid.toFixed(2) + ' − £' + cost.toFixed(2) + ' = £' + change.toFixed(2) + '.' };
    }},
    { gen: function() {
        var n = pickFrom([2, 3, 4]);
        var each = randInt(3, 12) + pickFrom([0.25, 0.50, 0.75, 0.00]);
        each = parseFloat(each.toFixed(2));
        var total = parseFloat((n * each).toFixed(2));
        var opts = buildOpts('£' + total.toFixed(2), ['£' + (total - each).toFixed(2), '£' + (total + each).toFixed(2), '£' + (total - 1.00).toFixed(2)]);
        return { q: n + ' friends each pay £' + each.toFixed(2) + ' for a shared taxi. What is the total fare?',
                 opts: opts, c: 0,
                 e: n + ' × £' + each.toFixed(2) + ' = £' + total.toFixed(2) + '.' };
    }},
    { gen: function() {
        var base = pickFrom([50, 60, 70, 80, 100]);
        var extra = randInt(1, 19) + pickFrom([0.00, 0.10, 0.25, 0.50, 0.75]);
        extra = parseFloat(extra.toFixed(2));
        var total = parseFloat((base + extra).toFixed(2));
        var diff = extra;
        var opts = buildOpts('£' + diff.toFixed(2), ['£' + (diff + 1.00).toFixed(2), '£' + (base - diff).toFixed(2), '£' + (diff - 0.10 < 0 ? diff : diff - 0.10).toFixed(2)]);
        return { q: 'A weekly shopping bill is £' + total.toFixed(2) + '. How much more than £' + base + ' is this?',
                 opts: opts, c: 0,
                 e: '£' + total.toFixed(2) + ' − £' + base.toFixed(2) + ' = £' + diff.toFixed(2) + '.' };
    }},
    { gen: function() {
        var items = [
            parseFloat((randInt(1, 4) + pickFrom([0.10, 0.25, 0.45, 0.50, 0.75, 0.99])).toFixed(2)),
            parseFloat((randInt(1, 5) + pickFrom([0.10, 0.20, 0.45, 0.50, 0.75])).toFixed(2)),
            parseFloat((randInt(0, 2) + pickFrom([0.49, 0.59, 0.79, 0.99])).toFixed(2)),
            parseFloat((randInt(1, 3) + pickFrom([0.10, 0.25, 0.46, 0.50])).toFixed(2))
        ];
        var total = parseFloat((items[0] + items[1] + items[2] + items[3]).toFixed(2));
        var opts = buildOpts('£' + total.toFixed(2), ['£' + (total - 0.10).toFixed(2), '£' + (total + 0.10).toFixed(2), '£' + (total - 1.00).toFixed(2)]);
        return { q: 'Four items cost £' + items[0].toFixed(2) + ', £' + items[1].toFixed(2) + ', £' + items[2].toFixed(2) + ' and £' + items[3].toFixed(2) + '. What is the total?',
                 opts: opts, c: 0,
                 e: '£' + items[0].toFixed(2) + ' + £' + items[1].toFixed(2) + ' + £' + items[2].toFixed(2) + ' + £' + items[3].toFixed(2) + ' = £' + total.toFixed(2) + '.' };
    }},
    { gen: function() {
        var big = parseFloat((randInt(100, 300) + pickFrom([0.00, 0.20, 0.40, 0.60, 0.80])).toFixed(2));
        var small = parseFloat((randInt(10, 50) + pickFrom([0.00, 0.20, 0.40, 0.60])).toFixed(2));
        var diff = parseFloat((big - small).toFixed(2));
        var opts = buildOpts('£' + diff.toFixed(2), ['£' + (diff - 1.00).toFixed(2), '£' + (big + small).toFixed(2), '£' + (diff + 1.00).toFixed(2)]);
        return { q: 'A family spends £' + big.toFixed(2) + ' on food and £' + small.toFixed(2) + ' on transport. What is the difference?',
                 opts: opts, c: 0,
                 e: '£' + big.toFixed(2) + ' − £' + small.toFixed(2) + ' = £' + diff.toFixed(2) + '.' };
    }},
    { gen: function() {
        var adult = randInt(6, 14) + pickFrom([0.00, 0.50]);
        var child = parseFloat((adult - randInt(2, 4) + pickFrom([0.00, 0.25, 0.75])).toFixed(2));
        adult = parseFloat(adult.toFixed(2));
        var na = pickFrom([2, 3]), nc = pickFrom([2, 3]);
        var total = parseFloat((na * adult + nc * child).toFixed(2));
        var opts = buildOpts('£' + total.toFixed(2), ['£' + (total - 1.00).toFixed(2), '£' + (total + 0.50).toFixed(2), '£' + (total + 1.00).toFixed(2)]);
        return { q: 'Cinema tickets cost £' + adult.toFixed(2) + ' for adults and £' + child.toFixed(2) + ' for children. ' + na + ' adults and ' + nc + ' children total:',
                 opts: opts, c: 0,
                 e: na + '×£' + adult.toFixed(2) + '+' + nc + '×£' + child.toFixed(2) + '=£' + (na*adult).toFixed(2) + '+£' + (nc*child).toFixed(2) + '=£' + total.toFixed(2) + '.' };
    }},
    { gen: function() {
        var balance = parseFloat((randInt(80, 250) + pickFrom([0.00, 0.20, 0.40, 0.60, 0.80])).toFixed(2));
        var withdraw = parseFloat((randInt(20, Math.floor(balance) - 10) + pickFrom([0.00, 0.05, 0.15, 0.45, 0.95])).toFixed(2));
        var remain = parseFloat((balance - withdraw).toFixed(2));
        var opts = buildOpts('£' + remain.toFixed(2), ['£' + (remain + 1.00).toFixed(2), '£' + (remain - 1.00).toFixed(2), '£' + (remain + 0.50).toFixed(2)]);
        return { q: 'A savings account has £' + balance.toFixed(2) + '. After withdrawing £' + withdraw.toFixed(2) + ', how much remains?',
                 opts: opts, c: 0,
                 e: '£' + balance.toFixed(2) + ' − £' + withdraw.toFixed(2) + ' = £' + remain.toFixed(2) + '.' };
    }}
  ],

  // ── mi-18-2: Sale Prices & Unit Prices ────────────────────────────────────
  'mi-18-2': [
    { gen: function() {
        var price = pickFrom([40, 50, 60, 80, 100, 120, 150]);
        var disc = pickFrom([10, 15, 20, 25, 30]);
        var ans = parseFloat((price * (1 - disc / 100)).toFixed(2));
        var discAmt = parseFloat((price * disc / 100).toFixed(2));
        var opts = buildOpts('£' + ans.toFixed(2), ['£' + (price - disc).toFixed(2), '£' + discAmt.toFixed(2), '£' + (price + discAmt).toFixed(2)]);
        return { q: 'A jacket costs £' + price + '. There is a ' + disc + '% discount. What is the sale price?',
                 opts: opts, c: 0,
                 e: disc + '% of £' + price + '=£' + discAmt.toFixed(2) + '. Sale price = £' + price + '−£' + discAmt.toFixed(2) + '=£' + ans.toFixed(2) + '.' };
    }},
    { gen: function() {
        var grams = pickFrom([200, 250, 400, 500, 750]);
        var pricePer100 = pickFrom([20, 25, 30, 40, 50]);
        var total = parseFloat((grams / 100 * pricePer100 / 100).toFixed(2));
        var opts = buildOpts(pricePer100 + 'p', [(pricePer100 - 5) + 'p', (pricePer100 + 5) + 'p', (pricePer100 * 2) + 'p']);
        return { q: 'A ' + grams + 'g pack costs £' + total.toFixed(2) + '. What is the cost per 100g?',
                 opts: opts, c: 0,
                 e: '£' + total.toFixed(2) + ' ÷ ' + (grams / 100) + ' = £' + (pricePer100 / 100).toFixed(2) + ' = ' + pricePer100 + 'p per 100g.' };
    }},
    { question:'Which is better value: 2 litres of juice for £1.80 or 3 litres for £2.55?', options:['3-litre bottle','2-litre bottle','Same value','Cannot tell'], answer:0, explanation:'2L: £1.80÷2=90p/L. 3L: £2.55÷3=85p/L. 3-litre is cheaper per litre.' },
    { gen: function() {
        var price = pickFrom([80, 100, 120, 150, 200]);
        var disc = pickFrom([15, 20, 25, 30, 35]);
        var discAmt = parseFloat((price * disc / 100).toFixed(2));
        var ans = parseFloat((price - discAmt).toFixed(2));
        var opts = buildOpts('£' + ans.toFixed(2), ['£' + (price - disc).toFixed(2), '£' + discAmt.toFixed(2), '£' + (ans + 5).toFixed(2)]);
        return { q: 'A £' + price + ' coat is reduced by ' + disc + '%. What is the new price?',
                 opts: opts, c: 0,
                 e: disc + '% of £' + price + '=£' + discAmt.toFixed(2) + '. New price=£' + price + '−£' + discAmt.toFixed(2) + '=£' + ans.toFixed(2) + '.' };
    }},
    { question:'A supermarket sells 400g of pasta for £1.20 and 600g for £1.68. Which is the better buy?', options:['600g pack','400g pack','Same price per gram','Cannot compare'], answer:0, explanation:'400g: 1.20÷400=0.3p/g. 600g: 1.68÷600=0.28p/g. 600g is cheaper per gram.' },
    { gen: function() {
        var orig = pickFrom([40, 50, 60, 80, 100]);
        var discPct = pickFrom([10, 15, 20, 25, 30]);
        var sale = parseFloat((orig * (1 - discPct / 100)).toFixed(2));
        var discAmt = parseFloat((orig - sale).toFixed(2));
        var opts = buildOpts(discPct + '%', [(discPct - 5) + '%', (discPct + 5) + '%', (discPct + 10) + '%']);
        return { q: 'Shoes are reduced from £' + orig + ' to £' + sale.toFixed(2) + '. What percentage is the discount?',
                 opts: opts, c: 0,
                 e: 'Discount=£' + discAmt.toFixed(2) + '. ' + discAmt.toFixed(2) + '÷' + orig + '×100=' + discPct + '%.' };
    }},
    { question:'A store has "buy 3 for the price of 2" on items costing £4.50 each. You buy 3. What do you pay?', options:['£9.00','£13.50','£6.75','£4.50'], answer:0, explanation:'Pay for 2 only: 2×£4.50=£9.00.' },
    { question:'Which is the best unit price? A: 10 for £3 B: 12 for £3.48 C: 15 for £4.20', options:['C (28p each)','A (30p each)','B (29p each)','All the same'], answer:0, explanation:'A=30p, B=29p, C=28p. C is cheapest per item.' },
    { gen: function() {
        var base = pickFrom([100, 150, 200, 250, 300]);
        var vatPct = 20;
        var vat = parseFloat((base * vatPct / 100).toFixed(2));
        var total = parseFloat((base + vat).toFixed(2));
        var opts = buildOpts('£' + total.toFixed(2), ['£' + (base + vatPct).toFixed(2), '£' + (base + base * 0.1).toFixed(2), '£' + (total + 10).toFixed(2)]);
        return { q: 'A laptop is listed at £' + base + ' plus 20% VAT. How much does it cost in total?',
                 opts: opts, c: 0,
                 e: 'VAT=20%×£' + base + '=£' + vat.toFixed(2) + '. Total=£' + base + '+£' + vat.toFixed(2) + '=£' + total.toFixed(2) + '.' };
    }},
    { gen: function() {
        var discPct = pickFrom([10, 20, 25]);
        var sale = pickFrom([27, 36, 45, 54, 60, 72, 80]);
        var orig = parseFloat((sale / (1 - discPct / 100)).toFixed(2));
        var opts = buildOpts('£' + orig.toFixed(2), ['£' + (sale + discPct).toFixed(2), '£' + (sale * (1 + discPct / 100)).toFixed(2), '£' + (sale - discPct).toFixed(2)]);
        return { q: 'An item costs £' + sale.toFixed(2) + ' after a ' + discPct + '% discount. What was the original price?',
                 opts: opts, c: 0,
                 e: 'Sale price = ' + (100 - discPct) + '% of original. Original = £' + sale.toFixed(2) + '÷' + (1 - discPct / 100) + '=£' + orig.toFixed(2) + '.' };
    }}
  ],

  // ── mi-18-3: VAT & Tips ────────────────────────────────────────────────────
  'mi-18-3': [
    { gen: function() {
        var base = pickFrom([20, 25, 30, 40, 50, 60]);
        var vatPct = 20;
        var vat = parseFloat((base * vatPct / 100).toFixed(2));
        var opts = buildOpts('£' + vat.toFixed(2), ['£' + (base * 0.1).toFixed(2), '£' + (base * 0.15).toFixed(2), '£' + (base * 0.25).toFixed(2)]);
        return { q: 'A meal costs £' + base + ' before VAT at 20%. What is the VAT amount?',
                 opts: opts, c: 0,
                 e: '20% of £' + base + ' = 0.2×£' + base + ' = £' + vat.toFixed(2) + '.' };
    }},
    { gen: function() {
        var base = pickFrom([100, 150, 200, 250, 300]);
        var vatPct = 20;
        var total = parseFloat((base * 1.2).toFixed(2));
        var opts = buildOpts('£' + total.toFixed(2), ['£' + (base + vatPct).toFixed(2), '£' + (base + base * 0.1).toFixed(2), '£' + (base + base * 0.3).toFixed(2)]);
        return { q: 'A phone costs £' + base + ' plus 20% VAT. What is the total price?',
                 opts: opts, c: 0,
                 e: 'VAT=£' + (base * 0.2).toFixed(2) + '. Total=£' + base + '+£' + (base * 0.2).toFixed(2) + '=£' + total.toFixed(2) + '. Or £' + base + '×1.2=£' + total.toFixed(2) + '.' };
    }},
    { gen: function() {
        var bill = pickFrom([30, 40, 45, 50, 60, 70]);
        var tipPct = pickFrom([10, 12, 15]);
        var tip = parseFloat((bill * tipPct / 100).toFixed(2));
        var opts = buildOpts('£' + tip.toFixed(2), ['£' + (tip + 0.50).toFixed(2), '£' + (tip - 0.50).toFixed(2), '£' + (bill * 0.20).toFixed(2)]);
        return { q: 'A restaurant bill is £' + bill + '. You leave a ' + tipPct + '% tip. What is the tip amount?',
                 opts: opts, c: 0,
                 e: tipPct + '% of £' + bill + ' = £' + tip.toFixed(2) + '.' };
    }},
    { gen: function() {
        var total = pickFrom([48, 60, 72, 84, 96, 120]);
        var vatPct = 20;
        var orig = parseFloat((total / 1.2).toFixed(2));
        var opts = buildOpts('£' + orig.toFixed(2), ['£' + (total * 0.8).toFixed(2), '£' + (total - vatPct).toFixed(2), '£' + (total * 0.85).toFixed(2)]);
        return { q: 'An item is priced at £' + total + ' including 20% VAT. What is the price before VAT?',
                 opts: opts, c: 0,
                 e: 'Price with VAT = 120% of original. Original = £' + total + '÷1.2 = £' + orig.toFixed(2) + '.' };
    }},
    { gen: function() {
        var base = pickFrom([40, 50, 60, 80, 100]);
        var tipPct = pickFrom([10, 15, 20]);
        var tip = parseFloat((base * tipPct / 100).toFixed(2));
        var total = parseFloat((base + tip).toFixed(2));
        var opts = buildOpts('£' + total.toFixed(2), ['£' + (base + tipPct).toFixed(2), '£' + (total - 2).toFixed(2), '£' + (total + 2).toFixed(2)]);
        return { q: 'A service costs £' + base + ' plus a ' + tipPct + '% tip. What is the total?',
                 opts: opts, c: 0,
                 e: 'Tip = ' + tipPct + '%×£' + base + '=£' + tip.toFixed(2) + '. Total=£' + base + '+£' + tip.toFixed(2) + '=£' + total.toFixed(2) + '.' };
    }},
    { gen: function() {
        var quote = pickFrom([200, 300, 400, 500, 600]);
        var vatPct = 20;
        var vat = parseFloat((quote * vatPct / 100).toFixed(2));
        var total = parseFloat((quote + vat).toFixed(2));
        var opts = buildOpts('£' + total.toFixed(2), ['£' + (quote + vatPct).toFixed(2), '£' + (total + 20).toFixed(2), '£' + (total - 20).toFixed(2)]);
        return { q: 'A builder quotes £' + quote + ' + VAT. VAT is 20%. What is the total bill?',
                 opts: opts, c: 0,
                 e: 'VAT=£' + vat.toFixed(2) + '. Total=£' + quote + '+£' + vat.toFixed(2) + '=£' + total.toFixed(2) + '.' };
    }},
    { gen: function() {
        var total = pickFrom([60, 72, 84, 96, 120, 144]);
        var vatPct = 20;
        var orig = parseFloat((total / 1.2).toFixed(2));
        var opts = buildOpts('£' + orig.toFixed(2), ['£' + (total * 0.8).toFixed(2), '£' + (total - vatPct).toFixed(2), '£' + (total * 0.85).toFixed(2)]);
        return { q: 'A tourist sees a price of £' + total + ' including VAT. What was the price excluding 20% VAT?',
                 opts: opts, c: 0,
                 e: '£' + total + ' ÷ 1.2 = £' + orig.toFixed(2) + '.' };
    }},
    { gen: function() {
        var bill = pickFrom([50, 60, 75, 80, 100]);
        var tipPct = 20;
        var tip = parseFloat((bill * tipPct / 100).toFixed(2));
        var total = parseFloat((bill + tip).toFixed(2));
        var opts = buildOpts('£' + total.toFixed(2), ['£' + (total + 5).toFixed(2), '£' + (total - 5).toFixed(2), '£' + (bill + bill * 0.1).toFixed(2)]);
        return { q: 'You want to tip 20% on a £' + bill + ' restaurant bill. How much in total do you pay?',
                 opts: opts, c: 0,
                 e: 'Tip = 20%×£' + bill + '=£' + tip.toFixed(2) + '. Total=£' + bill + '+£' + tip.toFixed(2) + '=£' + total.toFixed(2) + '.' };
    }},
    { gen: function() {
        var rate = pickFrom([60, 70, 75, 80, 85, 90]);
        var vatPct = 20;
        var vat = parseFloat((rate * vatPct / 100).toFixed(2));
        var total = parseFloat((rate + vat).toFixed(2));
        var opts = buildOpts('£' + total.toFixed(2), ['£' + (rate + vatPct).toFixed(2), '£' + (total + 5).toFixed(2), '£' + (total - 5).toFixed(2)]);
        return { q: 'A hotel adds VAT of 20% to a room rate of £' + rate + ' per night. What is the nightly cost with VAT?',
                 opts: opts, c: 0,
                 e: 'VAT=£' + vat.toFixed(2) + '. Total=£' + rate + '+£' + vat.toFixed(2) + '=£' + total.toFixed(2) + '.' };
    }},
    { question:'Which item has a higher VAT amount: a £300 camera or a £250 watch, both at 20% VAT?', options:['Camera (£60 VAT)','Watch (£50 VAT)','Same','Cannot tell'], answer:0, explanation:'Camera: 20%×£300=£60. Watch: 20%×£250=£50. Camera has higher VAT.' }
  ],

  // ── mi-18-4: Multiply & Divide Money & Coins ──────────────────────────────
  'mi-18-4': [
    { gen: function() {
        var pounds = randInt(1, 9);
        var pence = pickFrom([10, 15, 20, 25, 30, 35, 40, 45, 50, 55, 60, 75, 80, 95]);
        var qty = randInt(2, 8);
        var totalPence = (pounds * 100 + pence) * qty;
        var ans = (totalPence / 100).toFixed(2);
        var opts = buildOpts('£' + ans, ['£' + (parseFloat(ans) + 1).toFixed(2), '£' + (parseFloat(ans) - 1).toFixed(2), '£' + (pounds * qty) + '.00']);
        return { q: 'A notebook costs £' + pounds + '.' + (pence < 10 ? '0' : '') + pence + '. How much do ' + qty + ' cost?',
                 opts: opts, c: 0,
                 e: '£' + pounds + '.' + (pence < 10 ? '0' : '') + pence + ' × ' + qty + ' = £' + ans + '.' };
    }},
    { gen: function() {
        var people = pickFrom([2, 3, 4, 5, 6, 8]);
        var each = pickFrom([2.50, 3.25, 4.50, 5.75, 6.25, 7.50, 8.50]);
        var total = (each * people).toFixed(2);
        var opts = buildOpts('£' + each.toFixed(2), ['£' + (each + 1).toFixed(2), '£' + (each - 0.5).toFixed(2), '£' + total]);
        return { q: people + ' friends share a bill of £' + total + ' equally. How much does each pay?',
                 opts: opts, c: 0,
                 e: '£' + total + ' ÷ ' + people + ' = £' + each.toFixed(2) + ' each.' };
    }},
    { gen: function() {
        var n50 = randInt(1, 4), n20 = randInt(1, 5), n10 = randInt(0, 3);
        var total = n50 * 50 + n20 * 20 + n10 * 10;
        var totalStr = total >= 100 ? '£' + (total/100).toFixed(2) : total + 'p';
        var opts = buildOpts(totalStr, [(total + 10) + 'p', (total - 10) + 'p', (n50 + n20 + n10) + 'p']);
        return { q: 'You have ' + n50 + ' × 50p coins, ' + n20 + ' × 20p coins and ' + n10 + ' × 10p coins. What is the total?',
                 opts: opts, c: 0,
                 e: n50 + '×50p=' + (n50*50) + 'p + ' + n20 + '×20p=' + (n20*20) + 'p + ' + n10 + '×10p=' + (n10*10) + 'p = ' + total + 'p.' };
    }},
    { gen: function() {
        var price = pickFrom([1.99, 2.49, 2.99, 3.49, 3.99, 4.99]);
        var qty = randInt(3, 7);
        var totalPence = Math.round(price * 100) * qty;
        var ans = (totalPence / 100).toFixed(2);
        var opts = buildOpts('£' + ans, ['£' + (parseFloat(ans) + 1).toFixed(2), '£' + (parseFloat(ans) - 1).toFixed(2), '£' + (Math.ceil(price) * qty).toFixed(2)]);
        return { q: 'Pens cost £' + price.toFixed(2) + ' each. What is the total for ' + qty + ' pens?',
                 opts: opts, c: 0,
                 e: '£' + price.toFixed(2) + ' × ' + qty + ' = £' + ans + '.' };
    }},
    { gen: function() {
        var total = pickFrom([10, 15, 20, 24, 30, 36, 42]);
        var divisor = pickFrom([3, 4, 5, 6]);
        while (total % divisor !== 0) divisor = pickFrom([2, 3, 4, 5, 6]);
        var each = total / divisor;
        var opts = buildOpts('£' + each.toFixed(2), ['£' + (each + 1).toFixed(2), '£' + (each - 1).toFixed(2), '£' + total.toFixed(2)]);
        return { q: '£' + total + ' is shared equally among ' + divisor + ' children. How much does each get?',
                 opts: opts, c: 0,
                 e: '£' + total + ' ÷ ' + divisor + ' = £' + each.toFixed(2) + '.' };
    }},
    { question:'Sam has only 20p coins. He needs exactly £1.40. How many 20p coins does he need?', options:['7','6','8','14'], answer:0, explanation:'£1.40 = 140p. 140 ÷ 20 = 7 coins.' },
    { question:'A bag of 8 apples costs £2.40. What is the cost per apple?', options:['30p','£0.30','Both A and B','24p'], answer:2, explanation:'£2.40 ÷ 8 = £0.30 = 30p.' },
    { question:'You buy 5 items at £2.75 each and pay with a £20 note. What change do you get?', options:['£6.25','£7.25','£5.75','£6.75'], answer:0, explanation:'Total = 5 × £2.75 = £13.75. Change = £20 − £13.75 = £6.25.' },
    { question:'Which combination makes exactly £1? (A) 2×50p (B) 5×20p (C) 10×10p (D) All of these', options:['All of these','Only A','Only B','Only C'], answer:0, explanation:'A: 2×50=100p ✓. B: 5×20=100p ✓. C: 10×10=100p ✓.' },
    { question:'£27.50 is divided equally among 5 people. How much each?', options:['£5.50','£5.00','£6.50','£4.50'], answer:0, explanation:'£27.50 ÷ 5 = £5.50.' }
  ],

  // ── mi-19-1: Elapsed Time ──────────────────────────────────────────────────
  'mi-19-1': [
    { gen: function() {
        var startH = randInt(8, 15), startM = pickFrom([0, 5, 10, 15, 20, 30, 40, 45, 50]);
        var durM = pickFrom([20, 25, 30, 35, 40, 45, 50, 55]);
        var totalM = startH * 60 + startM + durM;
        var endH = Math.floor(totalM / 60), endM = totalM % 60;
        var fmt = function(h, m) { return h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = durM + ' minutes';
        var opts = buildOpts(ans, [(durM + 10) + ' minutes', (durM - 5) + ' minutes', (durM + 5) + ' minutes']);
        return { q: 'A lesson starts at ' + fmt(startH, startM) + ' and ends at ' + fmt(endH, endM) + '. How long is it?',
                 opts: opts, c: 0,
                 e: 'From ' + fmt(startH, startM) + ' to ' + fmt(endH, endM) + ' = ' + durM + ' minutes.' };
    }},
    { gen: function() {
        var h = randInt(1, 4), m = pickFrom([5, 10, 15, 20, 25, 30, 35, 40, 45]);
        var total = h * 60 + m;
        var opts = buildOpts(h + 'h ' + m + 'min', [(h + 1) + 'h ' + m + 'min', h + 'h ' + (m + 10) + 'min', (h - 1) + 'h ' + (m + 10) + 'min']);
        return { q: 'How many hours and minutes is ' + total + ' minutes?',
                 opts: opts, c: 0,
                 e: total + '÷60=' + h + ' remainder ' + m + '. So ' + h + ' hours ' + m + ' minutes.' };
    }},
    { gen: function() {
        var startH = randInt(6, 14), startM = pickFrom([0, 15, 30, 45]);
        var durH = randInt(0, 2), durM = pickFrom([15, 20, 25, 30, 45]);
        var totalMins = startH * 60 + startM + durH * 60 + durM;
        var endH = Math.floor(totalMins / 60), endM = totalMins % 60;
        var fmt = function(h, m) { return h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = fmt(endH, endM);
        var durStr = (durH > 0 ? durH + 'h ' : '') + durM + 'min';
        var elapsed = durH * 60 + durM;
        var elapsedH = Math.floor(elapsed / 60), elapsedM = elapsed % 60;
        var ansStr = (elapsedH > 0 ? elapsedH + 'h ' : '') + elapsedM + 'min';
        var opts = buildOpts(ansStr, [(elapsedH + 1) + 'h ' + elapsedM + 'min', (elapsedH > 0 ? elapsedH + 'h ' : '') + (elapsedM + 15) + 'min', elapsedH + 'h ' + (elapsedM > 0 ? elapsedM - 15 : 45) + 'min']);
        return { q: 'A bus departs at ' + fmt(startH, startM) + ' and arrives at ' + ans + '. What is the journey time?',
                 opts: opts, c: 0,
                 e: fmt(startH, startM) + ' to ' + ans + ' = ' + ansStr + '.' };
    }},
    { gen: function() {
        var startH = randInt(10, 17), startM = pickFrom([0, 10, 15, 20, 30, 40, 50]);
        var durH = randInt(1, 2), durM = pickFrom([10, 15, 20, 30, 40, 50]);
        var totalMins = startH * 60 + startM + durH * 60 + durM;
        var endH = Math.floor(totalMins / 60) % 24, endM = totalMins % 60;
        var fmt = function(h, m) { return h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = fmt(endH, endM);
        var durStr = durH + 'h ' + durM + 'min';
        var opts = buildOpts(ans, [fmt(endH + 1, endM), fmt(endH, (endM + 10) % 60), fmt(endH - 1, endM)]);
        return { q: 'A film starts at ' + fmt(startH, startM) + ' and lasts ' + durStr + '. When does it end?',
                 opts: opts, c: 0,
                 e: fmt(startH, startM) + '+' + durH + 'h=' + fmt(startH + durH, startM) + '. ' + fmt(startH + durH, startM) + '+' + durM + 'min=' + ans + '.' };
    }},
    { gen: function() {
        var startH = randInt(7, 10), startM = pickFrom([0, 15, 30, 45, 55]);
        var endH = randInt(14, 17), endM = pickFrom([0, 10, 15, 20, 25, 30, 45]);
        var fmt = function(h, m) { return h + ':' + (m < 10 ? '0' : '') + m; };
        var totalStart = startH * 60 + startM, totalEnd = endH * 60 + endM;
        var diff = totalEnd - totalStart;
        var dH = Math.floor(diff / 60), dM = diff % 60;
        var ans = dH + 'h ' + dM + 'min';
        var opts = buildOpts(ans, [(dH + 1) + 'h ' + dM + 'min', dH + 'h ' + (dM + 10) + 'min', (dH - 1) + 'h ' + (dM + 30) + 'min']);
        return { q: 'A school day runs from ' + fmt(startH, startM) + ' to ' + fmt(endH, endM) + '. How long is it?',
                 opts: opts, c: 0,
                 e: fmt(startH, startM) + ' to ' + fmt(endH, endM) + ' = ' + ans + '.' };
    }},
    { gen: function() {
        var landH = randInt(13, 22), landM = pickFrom([0, 15, 20, 30, 45]);
        var durH = randInt(1, 4), durM = pickFrom([15, 20, 25, 30]);
        var totalLand = landH * 60 + landM;
        var totalDepart = totalLand - (durH * 60 + durM);
        var depH = Math.floor(totalDepart / 60), depM = totalDepart % 60;
        var fmt = function(h, m) { return h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = fmt(depH, depM);
        var opts = buildOpts(ans, [fmt(depH + 1, depM), fmt(depH, (depM + 15) % 60), fmt(depH - 1, depM)]);
        return { q: 'A plane lands at ' + fmt(landH, landM) + ' after a ' + durH + 'h ' + durM + 'min flight. What time did it take off?',
                 opts: opts, c: 0,
                 e: fmt(landH, landM) + '−' + durH + 'h=' + fmt(landH - durH, landM) + '. ' + fmt(landH - durH, landM) + '−' + durM + 'min=' + ans + '.' };
    }},
    { gen: function() {
        var startH = randInt(6, 10), startM = pickFrom([0, 10, 15, 20, 30, 40, 45]);
        var endH = startH + randInt(1, 3), endM = pickFrom([0, 5, 10, 15, 20, 25, 30]);
        var fmt = function(h, m) { return h + ':' + (m < 10 ? '0' : '') + m; };
        var diff = (endH * 60 + endM) - (startH * 60 + startM);
        var ans = diff + ' minutes';
        var opts = buildOpts(ans, [(diff + 10) + ' minutes', (diff - 10) + ' minutes', (diff + 20) + ' minutes']);
        return { q: 'How many minutes are there between ' + fmt(startH, startM) + ' am and ' + fmt(endH, endM) + ' am?',
                 opts: opts, c: 0,
                 e: fmt(startH, startM) + ' to ' + fmt(endH, endM) + ': ' + Math.floor(diff / 60) + 'h ' + (diff % 60) + 'min = ' + diff + ' minutes.' };
    }},
    { gen: function() {
        var startH = randInt(9, 14), startM = pickFrom([0, 10, 15, 20, 25, 30, 35, 40]);
        var durH = randInt(0, 1), durM = pickFrom([15, 20, 25, 30, 35, 40, 45]);
        var totalMins = startH * 60 + startM + durH * 60 + durM;
        var endH = Math.floor(totalMins / 60) % 24, endM = totalMins % 60;
        var fmt = function(h, m) { return h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = fmt(endH, endM);
        var durStr = (durH > 0 ? durH + 'h ' : '') + durM + 'min';
        var opts = buildOpts(ans, [fmt(endH, (endM + 10) % 60), fmt(endH + 1, endM), fmt(endH, Math.max(0, endM - 10))]);
        return { q: 'A cake goes in the oven at ' + fmt(startH, startM) + ' and needs to bake for ' + durStr + '. When should it come out?',
                 opts: opts, c: 0,
                 e: fmt(startH, startM) + '+' + durStr + '=' + ans + '.' };
    }},
    { gen: function() {
        var startH = randInt(8, 10), endH = randInt(16, 18);
        var startM = pickFrom([0, 15, 30]), endM = pickFrom([0, 15, 30, 45]);
        var totalWork = (endH * 60 + endM) - (startH * 60 + startM);
        var lunchM = pickFrom([30, 45, 60]);
        var netM = totalWork - lunchM;
        var netH = Math.floor(netM / 60), netMm = netM % 60;
        var fmt = function(h, m) { return h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = netH + 'h ' + netMm + 'min';
        var totalH = Math.floor(totalWork / 60), totalMm = totalWork % 60;
        var opts = buildOpts(ans, [totalH + 'h ' + totalMm + 'min', (netH + 1) + 'h ' + netMm + 'min', netH + 'h ' + (netMm + 15) + 'min']);
        return { q: 'Alice works from ' + fmt(startH, startM) + ' to ' + fmt(endH, endM) + ', with a ' + lunchM + '-minute lunch break. How long does she work?',
                 opts: opts, c: 0,
                 e: fmt(startH, startM) + ' to ' + fmt(endH, endM) + ' = ' + totalH + 'h ' + totalMm + 'min. ' + totalH + 'h ' + totalMm + 'min−' + lunchM + 'min=' + ans + '.' };
    }},
    { gen: function() {
        var startH = randInt(9, 14), startM = randInt(0, 59);
        var durTotalSec = randInt(20, 55) * 60 + pickFrom([15, 20, 25, 30, 45]);
        var durM = Math.floor(durTotalSec / 60), durS = durTotalSec % 60;
        var endTotalSec = (startH * 3600 + startM * 60) + durTotalSec;
        var endH = Math.floor(endTotalSec / 3600), endM = Math.floor((endTotalSec % 3600) / 60), endS = endTotalSec % 60;
        var fmt2 = function(h, m, s) { return h + ':' + (m < 10 ? '0' : '') + m + ':' + (s < 10 ? '0' : '') + s; };
        var ans = durM + 'min ' + durS + 'sec';
        var opts = buildOpts(ans, [(durM + 1) + 'min ' + durS + 'sec', durM + 'min ' + (durS + 15) + 'sec', (durM - 1) + 'min ' + durS + 'sec']);
        return { q: 'A race starts at ' + fmt2(startH, startM, 0) + ' and the winner finishes at ' + fmt2(endH, endM, endS) + '. What is the winning time?',
                 opts: opts, c: 0,
                 e: 'From ' + fmt2(startH, startM, 0) + ' to ' + fmt2(endH, endM, endS) + ': ' + durM + 'min ' + durS + 'sec.' };
    }}
  ],

  // ── mi-19-2: 12-Hour & 24-Hour Time ───────────────────────────────────────
  'mi-19-2': [
    { gen: function() {
        var h12 = randInt(1, 11), m = pickFrom([0, 15, 20, 30, 45]);
        var period = pickFrom(['am', 'pm']);
        var h24 = period === 'pm' ? h12 + 12 : (h12 === 12 ? 0 : h12);
        var fmt24 = function(h, m) { return (h < 10 ? '0' : '') + h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = fmt24(h24, m);
        var opts = buildOpts(ans, [fmt24(h24 === h12 + 12 ? h12 : h12 + 12, m), fmt24((h24 + 1) % 24, m), fmt24(h24, (m + 15) % 60)]);
        return { q: 'Convert ' + h12 + ':' + (m < 10 ? '0' : '') + m + ' ' + period + ' to 24-hour time.',
                 opts: opts, c: 0,
                 e: (period === 'pm' ? 'pm times: add 12. ' + h12 + '+12=' + h24 : 'am times before 12: write with leading zero.') + ' Answer: ' + ans + '.' };
    }},
    { gen: function() {
        var h24 = randInt(13, 23), m = pickFrom([0, 10, 15, 20, 30, 40, 45]);
        var h12 = h24 - 12;
        var fmt12 = function(h, m) { return h + ':' + (m < 10 ? '0' : '') + m + ' pm'; };
        var ans = fmt12(h12, m);
        var opts = buildOpts(ans, [h12 + ':' + (m < 10 ? '0' : '') + m + ' am', (h12 + 1) + ':' + (m < 10 ? '0' : '') + m + ' pm', (h12 - 1) + ':' + (m < 10 ? '0' : '') + m + ' pm']);
        return { q: 'Convert ' + h24 + ':' + (m < 10 ? '0' : '') + m + ' to 12-hour time.',
                 opts: opts, c: 0,
                 e: h24 + '−12=' + h12 + '. So ' + ans + '.' };
    }},
    { gen: function() {
        var h12 = randInt(1, 11), m = pickFrom([0, 10, 15, 20, 30, 45]);
        var fmt24 = function(h, m) { return (h < 10 ? '0' : '') + h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = fmt24(h12, m);
        var opts = buildOpts(ans, [fmt24(h12 + 12, m), h12 + ':' + (m < 10 ? '0' : '') + m + ' am', fmt24(h12 + 1, m)]);
        return { q: 'Convert ' + h12 + ':' + (m < 10 ? '0' : '') + m + ' am to 24-hour time.',
                 opts: opts, c: 0,
                 e: 'am times before 12: write as is with leading zero. ' + ans + '.' };
    }},
    { question:'Which 24-hour time shows midnight?', options:['00:00','12:00','24:00','12:00 am'], answer:0, explanation:'Midnight = 00:00 in 24-hour format.' },
    { gen: function() {
        var h12 = randInt(1, 11), m = pickFrom([0, 10, 15, 20, 30, 45, 55]);
        var h24 = h12 + 12;
        var fmt24 = function(h, m) { return h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = fmt24(h24, m);
        var opts = buildOpts(ans, [fmt24(h12, m), fmt24(h24 + 1, m), fmt24(h24 - 1, m)]);
        return { q: 'A train departs at ' + h12 + ':' + (m < 10 ? '0' : '') + m + ' pm. In 24-hour time this is:',
                 opts: opts, c: 0,
                 e: h12 + ':' + (m < 10 ? '0' : '') + m + ' pm: ' + h12 + '+12=' + h24 + '. Answer: ' + ans + '.' };
    }},
    { gen: function() {
        var m = pickFrom([5, 10, 15, 20, 25, 30, 40, 45]);
        var ans = '12:' + (m < 10 ? '0' : '') + m + ' am';
        var opts = buildOpts(ans, ['12:' + (m < 10 ? '0' : '') + m + ' pm', '1:' + (m < 10 ? '0' : '') + m + ' am', '0:' + (m < 10 ? '0' : '') + m + ' am']);
        return { q: 'Convert 00:' + (m < 10 ? '0' : '') + m + ' to 12-hour time.',
                 opts: opts, c: 0,
                 e: '00:' + (m < 10 ? '0' : '') + m + ' is ' + m + ' minutes after midnight = ' + ans + '.' };
    }},
    { question:'A timetable shows three departure times: 09:20, 12:05, 15:40. Which is in the afternoon?', options:['12:05 and 15:40','15:40 only','All three','09:20 only'], answer:0, explanation:'12:00 onwards is pm/afternoon. 12:05 and 15:40 are both afternoon.' },
    { question:'Which time is latest in the day: 11:55 pm, 00:05, 23:58, 12:01 am?', options:['23:58','11:55 pm','12:01 am','All the same'], answer:0, explanation:'23:58 is 11:58 pm, which is later than 11:55 pm. 00:05 and 12:01 am are just after midnight.' },
    { gen: function() {
        var h24 = randInt(13, 21), m = pickFrom([0, 5, 10, 15, 20, 30, 45]);
        var h12 = h24 - 12;
        var ans = h12 + ':' + (m < 10 ? '0' : '') + m + ' pm';
        var opts = buildOpts(ans, [h12 + ':' + (m < 10 ? '0' : '') + m + ' am', (h12 + 1) + ':' + (m < 10 ? '0' : '') + m + ' pm', (h12 - 1) + ':' + (m < 10 ? '0' : '') + m + ' pm']);
        return { q: 'Convert ' + h24 + ':' + (m < 10 ? '0' : '') + m + ' to 12-hour time.',
                 opts: opts, c: 0,
                 e: h24 + '−12=' + h12 + '. So ' + ans + '.' };
    }},
    { gen: function() {
        var startH = randInt(7, 11), startM = pickFrom([0, 10, 20, 30, 40, 50]);
        var durH = 1, durM = pickFrom([10, 15, 20, 25, 30, 35, 40]);
        var totalMins = startH * 60 + startM + durH * 60 + durM;
        var endH = Math.floor(totalMins / 60) % 24, endM = totalMins % 60;
        var fmt = function(h, m) { return (h < 10 ? '0' : '') + h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = fmt(endH, endM);
        var opts = buildOpts(ans, [fmt(startH, (startM + durM) % 60), fmt(endH + 1, endM), fmt(endH, (endM + 10) % 60)]);
        return { q: 'A meeting starts at ' + fmt(startH, startM) + ' and lasts ' + durH + 'h ' + durM + 'min. Using 24-hour time, when does it end?',
                 opts: opts, c: 0,
                 e: fmt(startH, startM) + '+' + durH + 'h=' + fmt(startH + durH, startM) + '. ' + fmt(startH + durH, startM) + '+' + durM + 'min=' + ans + '.' };
    }}
  ],

  // ── mi-19-3: Time Zones & Schedules ──────────────────────────────────────
  'mi-19-3': [
    { gen: function() {
        var londonH = randInt(8, 18), londonM = pickFrom([0, 15, 30, 45]);
        var offset = pickFrom([1, 2, 3]);
        var cities = {1: 'Paris (GMT+1)', 2: 'Cairo (GMT+2)', 3: 'Moscow (GMT+3)'};
        var city = cities[offset];
        var localH = londonH + offset;
        var fmt = function(h, m) { return h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = fmt(localH, londonM);
        var opts = buildOpts(ans, [fmt(londonH - offset, londonM), fmt(londonH, londonM), fmt(localH + 1, londonM)]);
        return { q: 'London is GMT+0, ' + city + '. When it is ' + fmt(londonH, londonM) + ' in London, what time is it in ' + city.split(' ')[0] + '?',
                 opts: opts, c: 0,
                 e: city.split(' ')[0] + ' is ' + offset + ' hour' + (offset > 1 ? 's' : '') + ' ahead of London. ' + fmt(londonH, londonM) + '+' + offset + '=' + ans + '.' };
    }},
    { gen: function() {
        var londonH = randInt(5, 14), londonM = pickFrom([0, 15, 30, 45]);
        var offset = randInt(5, 9);
        var localH = (londonH + offset) % 24;
        var fmt = function(h, m) { return h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = fmt(localH, londonM);
        var opts = buildOpts(ans, [fmt((londonH + offset + 6) % 24, londonM), fmt(localH + 1, londonM), fmt(Math.abs(londonH - offset), londonM)]);
        return { q: 'A city is GMT+' + offset + '. When it is ' + fmt(londonH, londonM) + ' in London (GMT+0), what time is it there?',
                 opts: opts, c: 0,
                 e: fmt(londonH, londonM) + '+' + offset + '=' + ans + '.' };
    }},
    { question:'A flight leaves London at 14:00 and arrives in New York (GMT−5) at 17:00 local time. How long is the flight?', options:['8 hours','3 hours','7 hours','5 hours'], answer:0, explanation:'New York is 5h behind. 17:00 NY = 22:00 London time. 22:00−14:00=8 hours.' },
    { gen: function() {
        var depH = randInt(6, 18), depM = pickFrom([5, 10, 15, 20, 22, 30, 35, 40, 47, 52]);
        var durH = randInt(1, 3), durM = pickFrom([10, 15, 20, 25, 30, 36, 46]);
        var totalMins = depH * 60 + depM + durH * 60 + durM;
        var arrH = Math.floor(totalMins / 60) % 24, arrM = totalMins % 60;
        var fmt = function(h, m) { return (h < 10 ? '0' : '') + h + ':' + (m < 10 ? '0' : '') + m; };
        var elapsedM = durH * 60 + durM;
        var eH = durH, eM = durM;
        var ans = eH + 'h ' + eM + 'min';
        var opts = buildOpts(ans, [(eH + 1) + 'h ' + eM + 'min', eH + 'h ' + (eM + 10) + 'min', (eH - 1) + 'h ' + (eM + 30) + 'min']);
        return { q: 'A train timetable shows: Dep ' + fmt(depH, depM) + ', Arr ' + fmt(arrH, arrM) + '. How long is the journey?',
                 opts: opts, c: 0,
                 e: 'From ' + fmt(depH, depM) + ' to ' + fmt(arrH, arrM) + ': ' + ans + '.' };
    }},
    { gen: function() {
        var londonH = randInt(6, 16), londonM = pickFrom([0, 15, 30, 45]);
        var offset = pickFrom([3, 4, 5]);
        var cityNames = {3: 'Nairobi (GMT+3)', 4: 'Dubai (GMT+4)', 5: 'Islamabad (GMT+5)'};
        var city = cityNames[offset];
        var localH = londonH + offset;
        var fmt = function(h, m) { return h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = fmt(localH, londonM);
        var opts = buildOpts(ans, [fmt(londonH - offset, londonM), fmt(localH + 1, londonM), fmt(localH - 1, londonM)]);
        return { q: city.split(' ')[0] + ' is GMT+' + offset + '. A person in London calls ' + city.split(' ')[0] + ' at ' + fmt(londonH, londonM) + ' London time. What time is it there?',
                 opts: opts, c: 0,
                 e: city.split(' ')[0] + ' is ' + offset + ' hours ahead: ' + fmt(londonH, londonM) + '+' + offset + '=' + ans + '.' };
    }},
    { gen: function() {
        var depH = randInt(8, 16), depM = pickFrom([0, 7, 12, 17, 22, 30, 37, 42, 47, 52]);
        var durH = randInt(1, 2), durM = pickFrom([10, 15, 20, 25, 30, 35, 40, 45]);
        var totalMins = depH * 60 + depM + durH * 60 + durM;
        var arrH = Math.floor(totalMins / 60) % 24, arrM = totalMins % 60;
        var fmt = function(h, m) { return (h < 10 ? '0' : '') + h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = durH + 'h ' + durM + 'min';
        var opts = buildOpts(ans, [(durH + 1) + 'h ' + durM + 'min', durH + 'h ' + (durM + 10) + 'min', durH + 'h ' + Math.max(0, durM - 10) + 'min']);
        return { q: 'A bus timetable: Dep ' + fmt(depH, depM) + ', Arr ' + fmt(arrH, arrM) + '. How long is the journey?',
                 opts: opts, c: 0,
                 e: fmt(depH, depM) + ' to ' + fmt(arrH, arrM) + '=' + ans + '.' };
    }},
    { question:'Sydney is GMT+10. When it is 20:00 in London, what is the date/time in Sydney if it is Monday in London?', options:['06:00 Tuesday','06:00 Monday','18:00 Monday','10:00 Tuesday'], answer:0, explanation:'20:00+10=30:00=06:00 next day. So 06:00 Tuesday.' },
    { gen: function() {
        var trains = [];
        var base = randInt(7, 12) * 60 + pickFrom([0, 5, 10, 15]);
        for (var i = 0; i < 4; i++) { trains.push(base + i * pickFrom([20, 25, 30])); }
        var arrivalIdx = 1;
        var arrival = trains[arrivalIdx] + randInt(3, 12);
        var nextTrain = trains[arrivalIdx + 1];
        var fmt = function(total) { var h = Math.floor(total / 60), m = total % 60; return (h < 10 ? '0' : '') + h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = fmt(nextTrain);
        var opts = buildOpts(ans, [fmt(trains[arrivalIdx]), fmt(trains[arrivalIdx - 1]), 'None']);
        return { q: 'A timetable shows trains at ' + [0,1,2,3].map(function(i){ return fmt(trains[i]); }).join(', ') + '. You arrive at the station at ' + fmt(arrival) + '. Which train can you catch?',
                 opts: opts, c: 0,
                 e: 'You arrive at ' + fmt(arrival) + ', so the ' + fmt(trains[arrivalIdx]) + ' has already left. The next train is ' + ans + '.' };
    }},
    { gen: function() {
        var londonH = randInt(10, 20), londonM = pickFrom([0, 15, 30, 45]);
        var offset = pickFrom([5, 6, 7, 8]);
        var localH = londonH - offset;
        var fmt = function(h, m) { return (h < 10 ? '0' : '') + h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = fmt(localH, londonM);
        var opts = buildOpts(ans, [fmt(londonH + offset, londonM), fmt(localH + 1, londonM), fmt(localH - 1, londonM)]);
        return { q: 'A city is GMT−' + offset + '. It is ' + fmt(londonH, londonM) + ' in London (GMT+0). What time is it there?',
                 opts: opts, c: 0,
                 e: fmt(londonH, londonM) + '−' + offset + '=' + ans + '.' };
    }},
    { gen: function() {
        var depH = randInt(20, 23), depM = pickFrom([5, 10, 15, 20, 25, 30, 35, 40, 45, 48]);
        var durH = randInt(1, 3), durM = pickFrom([10, 15, 17, 20, 27, 30]);
        var totalMins = depH * 60 + depM + durH * 60 + durM;
        var arrH = Math.floor(totalMins / 60) % 24, arrM = totalMins % 60;
        var fmt = function(h, m) { return (h < 10 ? '0' : '') + h + ':' + (m < 10 ? '0' : '') + m; };
        var ans = durH + 'h ' + durM + 'min';
        var opts = buildOpts(ans, [(durH + 1) + 'h ' + durM + 'min', durH + 'h ' + (durM + 6) + 'min', (durH - 1) + 'h ' + durM + 'min']);
        return { q: 'A train departs at ' + fmt(depH, depM) + ' and arrives at ' + fmt(arrH, arrM) + ' the next day. How long is the journey?',
                 opts: opts, c: 0,
                 e: fmt(depH, depM) + ' to midnight=' + (24 * 60 - (depH * 60 + depM)) + 'min. Midnight to ' + fmt(arrH, arrM) + '=' + (arrH * 60 + arrM) + 'min. Total=' + ans + '.' };
    }}
  ],

  // ── mi-20-1: Plot Points on a Coordinate Plane ─────────────────────────────
  'mi-20-1': [
    { gen: function() {
        var x = randInt(1, 8), y = randInt(1, 8);
        var ans = '(' + x + ', ' + y + ')';
        var opts = buildOpts(ans, ['(' + y + ', ' + x + ')', '(−' + x + ', ' + y + ')', '(' + x + ', −' + y + ')']);
        return { q: 'Which coordinate pair describes a point ' + x + ' right and ' + y + ' up from the origin?',
                 opts: opts, c: 0,
                 e: 'x=' + x + ' (right), y=' + y + ' (up). Coordinate: (' + x + ', ' + y + ').' };
    }},
    { question:'Point A is at (−2, 5). What does −2 represent?', options:['2 units left of the y-axis','2 units right of the y-axis','2 units below the x-axis','2 units above the x-axis'], answer:0, explanation:'The x-coordinate is −2, meaning 2 units to the left of the y-axis.' },
    { question:'Which point lies on the y-axis?', options:['(0, 4)','(4, 0)','(2, 3)','(−4, 0)'], answer:0, explanation:'Points on the y-axis have x=0. (0, 4) has x=0, so it lies on the y-axis.' },
    { question:'What are the coordinates of the origin?', options:['(0, 0)','(1, 1)','(0, 1)','(1, 0)'], answer:0, explanation:'The origin is always (0, 0).' },
    { question:'In the coordinate (−3, −7), which axis does −3 refer to?', options:['x-axis','y-axis','Both','Neither'], answer:0, explanation:'The first value in a coordinate pair always refers to the x-axis.' },
    { gen: function() {
        var x = randInt(1, 8), y = randInt(1, 8);
        var ans = '(' + x + ', −' + y + ')';
        var opts = buildOpts(ans, ['(−' + y + ', ' + x + ')', '(' + x + ', ' + y + ')', '(−' + x + ', ' + y + ')']);
        return { q: 'Which point is ' + y + ' units below the x-axis and ' + x + ' units to the right?',
                 opts: opts, c: 0,
                 e: x + ' right = x=' + x + '. ' + y + ' below = y=−' + y + '. Coordinate: (' + x + ', −' + y + ').' };
    }},
    { question:'A shape has vertices at (1,1), (4,1), (4,4) and (1,4). What shape is it?', options:['Square','Rectangle','Triangle','Trapezium'], answer:0, explanation:'All sides equal length 3 units. Four right angles. It is a square.' },
    { gen: function() {
        var x = randInt(1, 7), y = randInt(1, 7);
        var ans = '(−' + x + ', ' + y + ')';
        var opts = buildOpts(ans, ['(' + x + ', −' + y + ')', '(−' + x + ', −' + y + ')', '(' + y + ', ' + x + ')']);
        return { q: 'Point B is reflected in the y-axis from (' + x + ', ' + y + '). What are its new coordinates?',
                 opts: opts, c: 0,
                 e: 'Reflecting in the y-axis changes the sign of the x-coordinate only. (' + x + ',' + y + ')→(−' + x + ',' + y + ').' };
    }},
    { question:'Which of these points is NOT in quadrant I?', options:['(−1, 2)','(3, 4)','(1, 1)','(2, 5)'], answer:0, explanation:'Quadrant I requires both x and y positive. (−1, 2) has x negative, so it is NOT in Quadrant I.' },
    { gen: function() {
        var x1 = randInt(-6, -1), x2 = randInt(1, 6), y = randInt(-4, 6);
        var dist = x2 - x1;
        var opts = buildOpts('Right ' + dist + ' units', ['Left ' + dist + ' units', 'Up ' + dist + ' units', 'Down ' + dist + ' units']);
        return { q: 'A point moves from (' + x1 + ', ' + y + ') to (' + x2 + ', ' + y + '). What direction did it move and by how many units?',
                 opts: opts, c: 0,
                 e: 'y stays same. x: ' + x1 + '→' + x2 + ' is an increase of ' + dist + ' units → moved right ' + dist + '.' };
    }}
  ],

  // ── mi-20-2: Quadrants & Axes ──────────────────────────────────────────────
  'mi-20-2': [
    { gen: function() {
        var quadrants = [
            { x: randInt(1,8), y: randInt(1,8), q: 'Quadrant I', sign: '(+, +)' },
            { x: -randInt(1,8), y: randInt(1,8), q: 'Quadrant II', sign: '(−, +)' },
            { x: -randInt(1,8), y: -randInt(1,8), q: 'Quadrant III', sign: '(−, −)' },
            { x: randInt(1,8), y: -randInt(1,8), q: 'Quadrant IV', sign: '(+, −)' }
        ];
        var pt = pickFrom(quadrants);
        var ans = pt.q;
        var others = quadrants.filter(function(q) { return q.q !== pt.q; });
        var opts = buildOpts(ans, [others[0].q, others[1].q, others[2].q]);
        return { q: 'In which quadrant is the point (' + pt.x + ', ' + pt.y + ')?',
                 opts: opts, c: 0,
                 e: 'x ' + (pt.x > 0 ? 'positive' : 'negative') + ', y ' + (pt.y > 0 ? 'positive' : 'negative') + ' → ' + pt.q + '.' };
    }},
    { question:'What are the signs of coordinates in Quadrant IV?', options:['(+, −)','(−, +)','(+, +)','(−, −)'], answer:0, explanation:'Quadrant IV (bottom-right): x is positive, y is negative.' },
    { question:'A point at (5, 0) lies on which axis?', options:['x-axis','y-axis','Both axes','Neither axis'], answer:0, explanation:'y=0 means the point is on the x-axis.' },
    { question:'The point (0, −6) lies on the y-axis. True or false?', options:['True','False','On the x-axis','At the origin'], answer:0, explanation:'Any point with x=0 lies on the y-axis. (0, −6) has x=0, so True.' },
    { question:'Which point is in Quadrant III?', options:['(−2, −5)','(2, −5)','(−2, 5)','(2, 5)'], answer:0, explanation:'Quadrant III: both negative. (−2, −5) fits.' },
    { question:'A point moves from Quadrant I to Quadrant II. What changed?', options:['The sign of x changed to negative','The sign of y changed to negative','Both signs changed','Neither changed'], answer:0, explanation:'Quadrant I (+, +) → Quadrant II (−, +): x sign changes to negative.' },
    { gen: function() {
        var pts = [
            { x: pickFrom([1,-1]), y: pickFrom([1,-1]) },
            { x: pickFrom([3,-3]), y: 0 },
            { x: 0, y: pickFrom([-4,4]) },
            { x: pickFrom([2,-2]), y: pickFrom([2,-2]) }
        ];
        var dists = pts.map(function(p) { return Math.sqrt(p.x*p.x + p.y*p.y); });
        var minDist = Math.min.apply(null, dists);
        var minIdx = dists.indexOf(minDist);
        var fmt = function(p) { return '(' + p.x + ', ' + p.y + ')'; };
        var ans = fmt(pts[minIdx]);
        var opts = buildOpts(ans, pts.filter(function(_, i) { return i !== minIdx; }).map(fmt));
        return { q: 'Which point is closest to the origin? ' + pts.map(fmt).join(', '),
                 opts: opts, c: 0,
                 e: 'Distance from origin: ' + pts.map(function(p, i) { return fmt(p) + '=√' + (p.x*p.x + p.y*p.y); }).join('. ') + '. ' + ans + ' is closest.' };
    }},
    { question:'In which quadrant would you find the point (−100, 0.001)?', options:['Quadrant II','Quadrant I','Quadrant III','On the x-axis'], answer:0, explanation:'x<0, y>0 → Quadrant II.' },
    { question:'How many quadrants are there in a coordinate plane?', options:['4','2','8','6'], answer:0, explanation:'A coordinate plane has 4 quadrants, numbered I–IV.' },
    { question:'A coordinate has a positive x and a negative y. In which quadrant does it lie?', options:['Quadrant IV','Quadrant I','Quadrant II','Quadrant III'], answer:0, explanation:'(+, −) → Quadrant IV (bottom-right).' }
  ],

  // ── mi-20-3: Coordinate Maps & Directions ─────────────────────────────────
  'mi-20-3': [
    { gen: function() {
        var x = randInt(-4, 6), y1 = randInt(-3, 4), y2 = randInt(y1 + 2, y1 + 8);
        var dist = y2 - y1;
        var opts = buildOpts(dist + ' units', [(dist - 1) + ' units', (dist + 1) + ' units', (y1 + y2) + ' units']);
        return { q: 'What is the distance between (' + x + ', ' + y1 + ') and (' + x + ', ' + y2 + ')?',
                 opts: opts, c: 0,
                 e: 'Same x, so vertical distance: |' + y2 + '−' + y1 + '|=' + dist + ' units.' };
    }},
    { gen: function() {
        var x = randInt(-6, 2), y = randInt(-3, 5), move = randInt(3, 10);
        var newX = x + move;
        var ans = '(' + newX + ', ' + y + ')';
        var opts = buildOpts(ans, ['(' + (x - move) + ', ' + y + ')', '(' + newX + ', ' + (y + move) + ')', '(' + x + ', ' + (y + move) + ')']);
        return { q: 'Point P is at (' + x + ', ' + y + '). Move right ' + move + ' units. What are the new coordinates?',
                 opts: opts, c: 0,
                 e: x + '+' + move + '=' + newX + '. y stays ' + y + '. New point: (' + newX + ', ' + y + ').' };
    }},
    { gen: function() {
        var x1 = randInt(-5, -1), x2 = randInt(1, 7), y = 0;
        var dist = x2 - x1;
        var opts = buildOpts(dist + ' units', [(x2 + x1) + ' units', (dist - 2) + ' units', (dist + 2) + ' units']);
        return { q: 'What is the distance between (' + x1 + ', 0) and (' + x2 + ', 0)?',
                 opts: opts, c: 0,
                 e: 'Same y=0, so horizontal distance: |' + x2 + '−(' + x1 + ')|=' + dist + ' units.' };
    }},
    { gen: function() {
        var x1 = randInt(0, 4), y = randInt(0, 5), x2 = x1 + randInt(2, 7);
        var dist = x2 - x1;
        var opts = buildOpts(dist + ' units', [(dist - 1) + ' units', (dist + 1) + ' units', (x1 + x2) + ' units']);
        return { q: 'A map uses a grid. School is at (' + x1 + ', ' + y + ') and park is at (' + x2 + ', ' + y + '). How far apart are they?',
                 opts: opts, c: 0,
                 e: 'Same y, so horizontal distance: |' + x2 + '−' + x1 + '|=' + dist + ' units.' };
    }},
    { gen: function() {
        var x = randInt(-3, 5), y = randInt(-2, 6), left = randInt(1, 5), down = randInt(1, 5);
        var newX = x - left, newY = y - down;
        var ans = '(' + newX + ', ' + newY + ')';
        var opts = buildOpts(ans, ['(' + (x + left) + ', ' + (y + down) + ')', '(' + newX + ', ' + (y + down) + ')', '(' + (x + left) + ', ' + newY + ')']);
        return { q: 'From point A at (' + x + ', ' + y + '), you move ' + left + ' left and ' + down + ' down. What are your new coordinates?',
                 opts: opts, c: 0,
                 e: 'x: ' + x + '−' + left + '=' + newX + '. y: ' + y + '−' + down + '=' + newY + '. New point: (' + newX + ', ' + newY + ').' };
    }},
    { question:'Two points share the same x-coordinate. Their distance apart equals:', options:['The difference in y-coordinates','The difference in x-coordinates','The sum of all coordinates','Cannot be determined'], answer:0, explanation:'Same x-coordinate means they are vertically aligned. Distance = |y₂ − y₁|.' },
    { gen: function() {
        var tx = randInt(2, 8), ty = randInt(-4, -1);
        var dist = tx;
        var opts = buildOpts(dist + ' units', [Math.abs(ty) + ' units', (dist + Math.abs(ty)) + ' units', (dist - 1) + ' units']);
        return { q: 'A grid map shows Treasure at (' + tx + ', ' + ty + ') and Start at (0, 0). What is the horizontal distance from Start to Treasure?',
                 opts: opts, c: 0,
                 e: 'Horizontal distance = |' + tx + '−0|=' + dist + ' units.' };
    }},
    { gen: function() {
        var x = randInt(-5, 0), y = randInt(-5, -1);
        var x2 = -x;
        var midX = 0, midY = y;
        var ans = '(' + midX + ', ' + midY + ')';
        var opts = buildOpts(ans, ['(' + midX + ', 0)', '(0, 0)', '(' + x + ', 0)']);
        return { q: 'Point A is at (' + x + ', ' + y + ') and point B at (' + x2 + ', ' + y + '). The midpoint of AB is:',
                 opts: opts, c: 0,
                 e: 'Midpoint x: (' + x + '+' + x2 + ')/2=' + midX + '. y: (' + y + '+' + y + ')/2=' + midY + '. Midpoint = (' + midX + ', ' + midY + ').' };
    }},
    { gen: function() {
        var x = randInt(-2, 4), y = randInt(1, 6), right = randInt(2, 5), up = randInt(1, 4);
        var newX = x + right, newY = y + up;
        var ans = '(' + newX + ', ' + newY + ')';
        var opts = buildOpts(ans, ['(' + newX + ', ' + (y - up) + ')', '(' + (x - right) + ', ' + newY + ')', '(' + newX + ', ' + (newY + 1) + ')']);
        return { q: 'You start at (' + x + ', ' + y + '), go right ' + right + ' and then up ' + up + '. Where are you now?',
                 opts: opts, c: 0,
                 e: 'Right ' + right + ': x=' + x + '+' + right + '=' + newX + '. Up ' + up + ': y=' + y + '+' + up + '=' + newY + '. Final: (' + newX + ', ' + newY + ').' };
    }},
    { gen: function() {
        var x1 = randInt(-4, 0), y1 = randInt(1, 5);
        var x2 = x1 + randInt(3, 8), y2 = y1;
        var x3 = x2, y3 = y2 - randInt(3, 8);
        var leg1 = x2 - x1, leg2 = Math.abs(y3 - y2);
        var total = leg1 + leg2;
        var opts = buildOpts(total + ' units', [leg1 + ' units', leg2 + ' units', (total + 2) + ' units']);
        return { q: 'A robot travels from (' + x1 + ', ' + y1 + ') to (' + x2 + ', ' + y2 + ') to (' + x3 + ', ' + y3 + '). What is the total distance travelled?',
                 opts: opts, c: 0,
                 e: 'Leg 1: |' + x2 + '−(' + x1 + ')|=' + leg1 + '. Leg 2: |' + y3 + '−' + y2 + '|=' + leg2 + '. Total=' + leg1 + '+' + leg2 + '=' + total + ' units.' };
    }}
  ],

  // ── mi-21-1: Arithmetic Sequences ─────────────────────────────────────────
  'mi-21-1': [
    { gen: function() {
        var start = randInt(2, 15), diff = pickFrom([2, 3, 4, 5, 6, 7, 8]);
        var terms = [start, start+diff, start+2*diff, start+3*diff];
        var next = start + 4*diff;
        var opts = buildOpts(next, [next + diff, next - diff, next + 1]);
        return { q: 'What is the next term in: ' + terms.join(', ') + ', __?',
                 opts: opts, c: 0,
                 e: 'Common difference = ' + diff + '. ' + (start+3*diff) + '+' + diff + '=' + next + '.' };
    }},
    { gen: function() {
        var start = randInt(20, 50), diff = -pickFrom([2, 3, 4, 5, 6, 7, 8, 10]);
        var terms = [start, start+diff, start+2*diff, start+3*diff];
        var ans = diff;
        var opts = buildOpts(ans, [-ans, ans - 1, ans + 1]);
        return { q: 'What is the common difference in: ' + terms.join(', ') + ', …?',
                 opts: opts, c: 0,
                 e: (start+diff) + '−' + start + '=' + diff + '. The sequence decreases by ' + Math.abs(diff) + ' each time.' };
    }},
    { gen: function() {
        var d = pickFrom([2, 3, 4, 5]);
        var c = randInt(-3, 5);
        var a = d + c;
        var terms = [a, a+d, a+2*d, a+3*d];
        var formula = d + 'n ' + (c >= 0 ? '+ ' + c : '− ' + Math.abs(c));
        var wrong1 = d + 'n ' + (c + 1 >= 0 ? '+ ' + (c+1) : '− ' + Math.abs(c+1));
        var wrong2 = (d+1) + 'n ' + (c >= 0 ? '+ ' + c : '− ' + Math.abs(c));
        var wrong3 = 'n + ' + d;
        var opts = buildOpts(formula, [wrong1, wrong2, wrong3]);
        return { q: 'Find the nth-term formula for: ' + terms.join(', ') + ', …',
                 opts: opts, c: 0,
                 e: 'd=' + d + '. ' + d + 'n at n=1 gives ' + d + ', but term 1 is ' + a + '. So ' + formula + '. Check: n=1 → ' + (d*1+c) + ' ✓' };
    }},
    { gen: function() {
        var d = randInt(2, 6), c = randInt(-3, 5);
        var n = randInt(5, 15);
        var ans = d * n + c;
        var opts = buildOpts(ans, [d * n + c + 1, d * (n - 1) + c, d * n + c - 1]);
        return { q: 'Using nth term = ' + d + 'n + ' + c + ', find the ' + n + 'th term.',
                 opts: opts, c: 0,
                 e: d + '(' + n + ')+' + c + '=' + (d*n) + '+' + c + '=' + ans + '.' };
    }},
    { question:'Is the sequence 1, 4, 9, 16, 25 arithmetic?', options:['No — differences are not constant','Yes — it increases','Yes — it is the square sequence','Cannot tell'], answer:0, explanation:'Differences: 3, 5, 7, 9 — not constant. Not arithmetic.' },
    { gen: function() {
        var a1 = randInt(5, 15), d = -pickFrom([2, 3, 4]);
        var n = randInt(4, 7);
        var ans = a1 + (n - 1) * d;
        var opts = buildOpts(ans, [ans + d, ans - d, a1 + n * d]);
        var terms = [];
        for (var i = 0; i < n; i++) { terms.push(a1 + i * d); }
        return { q: 'The 1st term of a sequence is ' + a1 + ' and common difference is ' + d + '. What is the ' + n + 'th term?',
                 opts: opts, c: 0,
                 e: terms.join(', ') + '. The ' + n + 'th term is ' + ans + '.' };
    }},
    { gen: function() {
        var d = pickFrom([3, 4, 5, 6]), c = randInt(-2, 4);
        var a1 = d + c;
        var target = a1 + randInt(5, 15) * d;
        var n = (target - c) / d;
        var opts = buildOpts(n + 'th', [(n - 1) + 'th', (n + 1) + 'th', (n + 2) + 'th']);
        return { q: 'Which term of the sequence ' + a1 + ', ' + (a1+d) + ', ' + (a1+2*d) + ', … is ' + target + '?',
                 opts: opts, c: 0,
                 e: 'nth term=' + d + 'n+' + c + '. ' + d + 'n+' + c + '=' + target + '→' + d + 'n=' + (target-c) + '→n=' + n + '.' };
    }},
    { question:'A sequence has nth term 2n − 5. Which is the first positive term?', options:['n=3 (term=1)','n=2 (term=−1)','n=4 (term=3)','n=5 (term=5)'], answer:0, explanation:'2n−5>0→n>2.5→n=3. Term: 2(3)−5=1.' },
    { question:'Two arithmetic sequences: A = 3, 7, 11, … and B = 1, 5, 9, … Do they share any terms?', options:['No — they share no terms','Yes — both include 9','Yes — they share 13','Yes — all terms match'], answer:0, explanation:'A: 3,7,11,15,19,… (odd multiples of 4 minus 1). B: 1,5,9,13,17,… Both have d=4 but different starting points 2 apart, so they never coincide.' },
    { question:'The sequence 100, 95, 90, … What is the first negative term?', options:['−5 (21st term)','0 (20th term)','−10 (22nd term)','5 (19th term)'], answer:0, explanation:'nth term=105−5n. Set <0: 105−5n<0→n>21. 22nd term=105−110=−5.' }
  ],

  // ── mi-21-2: Geometric Sequences ──────────────────────────────────────────
  'mi-21-2': [
    { gen: function() {
        var a1 = randInt(2, 6), r = pickFrom([2, 3, 4]);
        var terms = [a1, a1*r, a1*r*r, a1*r*r*r];
        var next = a1 * Math.pow(r, 4);
        var opts = buildOpts(next, [next * r, terms[3] * r - r, next - r]);
        return { q: 'What is the next term in: ' + terms.join(', ') + ', …?',
                 opts: opts, c: 0,
                 e: 'Common ratio = ' + r + '. ' + terms[3] + '×' + r + '=' + next + '.' };
    }},
    { gen: function() {
        var a1 = pickFrom([64, 80, 96, 128, 160]);
        var r = pickFrom([2, 4]);
        var terms = [a1, a1/r, a1/r/r, a1/r/r/r];
        var ratioStr = r === 2 ? '½' : '¼';
        var ans = '1/' + r;
        var opts = buildOpts(ratioStr, ['' + r, '−' + ratioStr, '1/' + (r * 2)]);
        return { q: 'What is the common ratio in: ' + terms.join(', ') + ', …?',
                 opts: opts, c: 0,
                 e: terms[1] + '÷' + terms[0] + '=' + ratioStr + '. Each term is divided by ' + r + '.' };
    }},
    { question:'Is 3, 6, 9, 12, 15 a geometric sequence?', options:['No — it is arithmetic','Yes — ratio is 2','Yes — ratio is 3','Cannot tell'], answer:0, explanation:'The differences are constant (3), not the ratios. It is arithmetic, not geometric.' },
    { gen: function() {
        var a1 = randInt(2, 6), r = pickFrom([2, 3]);
        var terms = [a1, a1*r, a1*r*r, a1*r*r*r];
        var ans = terms[3];
        var opts = buildOpts(ans, [terms[2], terms[3]*r, terms[1]]);
        return { q: 'Find the 4th term of a geometric sequence with first term ' + a1 + ' and ratio ' + r + '.',
                 opts: opts, c: 0,
                 e: terms[0] + ', ' + terms[1] + ', ' + terms[2] + ', ' + terms[3] + '. The 4th term is ' + ans + '.' };
    }},
    { question:'Which sequence is geometric?', options:['1, 3, 9, 27','1, 3, 5, 7','2, 4, 6, 8','5, 10, 15, 20'], answer:0, explanation:'1, 3, 9, 27: each term is ×3. The ratio is constant, so it is geometric.' },
    { gen: function() {
        var a1 = pickFrom([32, 48, 64, 96]);
        var terms = [a1, a1/2, a1/4, a1/8, a1/16, a1/32];
        var ans = terms[5];
        var opts = buildOpts(ans, [terms[4], terms[4]/2 - 0.25, terms[5] + 0.5]);
        return { q: 'A geometric sequence: ' + terms.slice(0,4).join(', ') + ', … What is the 6th term?',
                 opts: opts, c: 0,
                 e: 'r=½. ' + terms.slice(0,6).join(', ') + '. The 6th term is ' + ans + '.' };
    }},
    { gen: function() {
        var a1 = 1, r = pickFrom([2, 3, 4]);
        var terms = [a1, a1*r, a1*r*r, a1*r*r*r, a1*Math.pow(r,4)];
        var ans = terms[4];
        var opts = buildOpts(ans, [terms[3], terms[4]*r, terms[3]*r - 1]);
        return { q: 'A sequence starts at 1 and has common ratio ' + r + '. What is the 5th term?',
                 opts: opts, c: 0,
                 e: terms.join(', ') + '. 5th term = ' + ans + '.' };
    }},
    { question:'Which of these could be the common ratio of a geometric sequence that is decreasing (but positive)?', options:['½','2','−1','3'], answer:0, explanation:'A ratio between 0 and 1 (like ½) makes the sequence decrease while staying positive.' },
    { gen: function() {
        var a1 = randInt(10, 30), r = 2, n = 4;
        var ans = a1 * Math.pow(r, n);
        var opts = buildOpts(ans, [a1 * Math.pow(r, n-1), a1 * n * r, a1 * Math.pow(r, n) * 2]);
        return { q: 'A bacteria colony doubles every hour. It starts with ' + a1 + ' bacteria. How many after ' + n + ' hours?',
                 opts: opts, c: 0,
                 e: 'Geometric: r=2. After ' + n + ' hours: ' + a1 + '×2⁴=' + a1 + '×' + Math.pow(r,n) + '=' + ans + '.' };
    }},
    { question:'Identify the type: 5, −5, 5, −5, …', options:['Geometric with r=−1','Arithmetic with d=0','Geometric with r=1','Neither'], answer:0, explanation:'Ratio: −5÷5=−1. Each term is ×(−1). Geometric with r=−1.' }
  ],

  // ── mi-21-3: Expressions for Sequences ────────────────────────────────────
  'mi-21-3': [
    { gen: function() {
        var a = randInt(2, 6), b = randInt(-3, 5);
        var n = randInt(3, 8);
        var ans = a * n + b;
        var opts = buildOpts(ans, [a * n + b + 2, a * (n + 1) + b, a * n + b - 2]);
        return { q: 'Evaluate the expression ' + a + 'n ' + (b >= 0 ? '+ ' + b : '− ' + Math.abs(b)) + ' for n = ' + n + '.',
                 opts: opts, c: 0,
                 e: a + '(' + n + ')' + (b >= 0 ? '+' + b : b) + '=' + (a*n) + (b >= 0 ? '+' + b : b) + '=' + ans + '.' };
    }},
    { gen: function() {
        var d = pickFrom([2, 3, 4, 5]);
        var c = randInt(-2, 5);
        var a1 = d + c;
        var terms = [a1, a1+d, a1+2*d, a1+3*d];
        var formula = d + 'n ' + (c >= 0 ? '+ ' + c : '− ' + Math.abs(c));
        var wrong1 = d + 'n ' + (c >= 1 ? '+ ' + (c-1) : '− ' + (Math.abs(c)+1));
        var wrong2 = (d+1) + 'n ' + (c >= 0 ? '+ ' + c : '− ' + Math.abs(c));
        var wrong3 = 'n + ' + d;
        var opts = buildOpts(formula, [wrong1, wrong2, wrong3]);
        return { q: 'What is the nth-term rule for: ' + terms.join(', ') + ', …?',
                 opts: opts, c: 0,
                 e: 'd=' + d + '. At n=1: ' + d + '(1)=' + d + ', need ' + a1 + ', so +' + c + '. nth term = ' + formula + '. Check: ' + d + '(1)+' + c + '=' + a1 + ' ✓' };
    }},
    { gen: function() {
        var a = randInt(2, 5), b = randInt(1, 8);
        var n = randInt(4, 10);
        var ans = a * n + b;
        var opts = buildOpts(ans, [a * n + b + a, a * (n - 1) + b, a * n]);
        return { q: 'Using rule T(n) = ' + a + 'n + ' + b + ', what is T(' + n + ')?',
                 opts: opts, c: 0,
                 e: 'T(' + n + ')=' + a + '(' + n + ')+' + b + '=' + (a*n) + '+' + b + '=' + ans + '.' };
    }},
    { question:'Is 50 a term in the sequence with nth term 4n + 2?', options:['No','Yes — it is the 12th term','Yes — it is the 13th term','Yes — it is the 11th term'], answer:0, explanation:'4n+2=50→4n=48→n=12. Yes, it is the 12th term.' },
    { question:'Write the nth-term expression for: 10, 8, 6, 4, …', options:['12 − 2n','2n + 8','10 − 2n','−2n + 8'], answer:0, explanation:'d=−2. Start: at n=1, need 10. −2(1)+c=10→c=12. nth term=12−2n. Check: 12−2(2)=8 ✓' },
    { gen: function() {
        var a = randInt(2, 5), b = randInt(-5, 3);
        var target = randInt(10, 30);
        var n = (target - b) / a;
        // ensure integer n
        var adjustedTarget = a * Math.round(n) + b;
        n = Math.round(n);
        var opts = buildOpts(n, [n + 1, n - 1, n + 2]);
        return { q: 'A sequence has rule T(n) = ' + a + 'n ' + (b >= 0 ? '+ ' + b : '− ' + Math.abs(b)) + '. What is the value of n when T(n) = ' + adjustedTarget + '?',
                 opts: opts, c: 0,
                 e: a + 'n' + (b >= 0 ? '+' + b : b) + '=' + adjustedTarget + '→' + a + 'n=' + (adjustedTarget - b) + '→n=' + n + '.' };
    }},
    { question:'Which expression generates the sequence 1, 4, 9, 16, 25?', options:['n²','2n−1','n+1','n(n+1)'], answer:0, explanation:'n²: 1,4,9,16,25 ✓ These are the square numbers.' },
    { question:'The nth term of sequence A is 2n + 1 and sequence B is 3n − 1. What is the first term they share?', options:['5','7','11','9'], answer:0, explanation:'2n+1=3m−1. Try values: A: 3,5,7,9,11,… B: 2,5,8,11,… They share 5 (n=2 in A, m=2 in B). First shared term is 5.' },
    { question:'Sequence: 6, 11, 16, 21, … Susie says the nth term is 5n + 1. Is she correct?', options:['Yes','No — it should be 5n + 1 but starting at n=1 gives 6','No — it is 5n − 1','Cannot tell'], answer:0, explanation:'5(1)+1=6 ✓ 5(2)+1=11 ✓. Susie is correct.' },
    { question:'The nth term is 100 − 3n. For which value of n is the term first less than 10?', options:['n = 31','n = 30','n = 32','n = 29'], answer:0, explanation:'100−3n<10→3n>90→n>30. First integer: n=31. Check: 100−93=7<10 ✓' }
  ]

});
